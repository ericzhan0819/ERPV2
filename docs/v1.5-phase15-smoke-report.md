# ERPV2 v1.5 第 15 部分 Browser Smoke Report

日期：2026-07-28

狀態：工程端 Firefox／Desktop Chrome 預驗證通過；使用者 Desktop Chrome 人工操作與真實
iPhone Safari 驗收待完成，因此第 15 部分尚未正式勾選。

## 1. 驗證範圍

本輪依 `PLAN_v1.5.md` 第 15 部分驗證：

- Admin 建立 sales／manager 帳號、建立提示、username 與待修改密碼狀態。
- Email 首次登入、強制改密碼 route、後端 `409 PASSWORD_CHANGE_REQUIRED` gate。
- 強制頁 Theme Toggle、登出、錯誤目前密碼與成功修改。
- 我的帳號入口、名稱即時更新、username、重複 username、唯讀 Email／角色與密碼錯誤。
- username／Email／username 大小寫登入與通用登入錯誤。
- Admin Reset、兩個既有 stateful Session 各自於下次操作回 `401`、重新登入與再次強制改密碼。
- App config 對 Login、Sidebar 與 Browser title 的同步。
- Admin／manager／sales 既有主要路由、角色阻擋、Theme 與 390px Mobile 基本操作。

## 2. Firefox 工程預驗證

環境：

```text
Firefox 152 headless
每個瀏覽器使用獨立 geckodriver 與 profile
Laravel：http://127.0.0.1:8015
Vite：http://127.0.0.1:5195
Database：/tmp 獨立 SQLite
角色：admin／manager／sales
```

結果：

```text
38 checks passed
```

直接證據包含：

- Admin 建立 sales 與 manager，User list 顯示「尚未設定」與「需修改密碼」。
- sales 以 Email／預設密碼登入後固定進 `/change-password`。
- 直接輸入 `/dashboard` 回強制頁；`GET /api/dashboard/summary` 回
  `409 + PASSWORD_CHANGE_REQUIRED`。
- 錯誤目前密碼不可通過；正確修改後進 Dashboard。
- Header 進入我的帳號，名稱更新後即時同步；username 正規化為小寫。
- 第二位使用者設定相同 username 時顯示「此帳號名稱已被使用」。
- username、Email 與大寫 username 均可用同一新密碼登入；錯誤訊息維持通用。
- Admin Reset 後兩個既有 stateful Session 各自取得 `401`，再由正式 API client 操作導回 Login。
- 新預設密碼登入後重新進強制頁，完成第二次改密碼後恢復 Dashboard。
- Admin／manager／sales 主要路由可用，manager／sales 的管理員或敏感路由仍被阻擋。
- 390px Sidebar focus、body scroll lock、無水平 overflow、Theme 與 Account 基本操作通過。

## 3. Desktop Chrome 工程預驗證

使用 Google Chrome Labs 官方 Chrome for Testing stable：

```text
Google Chrome for Testing 151.0.7922.47
ChromeDriver 151.0.7922.47
Laravel：http://127.0.0.1:8016
Vite：http://127.0.0.1:5196
Database：/tmp 獨立 SQLite
```

結果：

```text
15 Chrome checks passed
```

涵蓋 Login config／Browser title、Admin Email 登入、Admin 主要路由、建立 sales、首次登入、
強制頁 Theme、Dashboard gate、修改預設密碼、我的帳號名稱與 username、Sales 主要路由、
username 大小寫登入與登出。

## 4. App Config 暫改驗證

驗證期間只暫時將 `frontend/src/config/app.ts` 五個欄位改為彼此可辨識的測試值，確認：

```text
Login 主標       PASS
Login 副標       PASS
Sidebar 短名稱   PASS
Browser title    PASS
```

測後已用 patch 還原正式值；`git diff` 確認沒有殘留 config 修改。

## 5. 自動回歸

```text
cd backend && php artisan test
PASS：632 tests；615 passed、17 environment-gated skipped、3163 assertions

cd frontend && npm test
PASS：16 files／77 tests

cd frontend && npm run lint
PASS：0 errors；2 個既有 Fast Refresh warnings

cd frontend && npm run typecheck
PASS

cd frontend && npm run build
PASS：production JS 523.35 kB（gzip 140.05 kB）
```

Build 保留既有大於 500 kB chunk warning，本部分沒有新增 dependency 或 runtime 程式。

## 6. 資料與環境安全

- Firefox 與 Chrome 各使用獨立 `/tmp` SQLite，沒有讀寫既有 MySQL／MariaDB 或開發資料。
- 所有 SQLite、driver、browser profile、下載的 Chrome binary 與測試腳本均已刪除。
- 獨立 Laravel／Vite／driver processes 均已停止。
- 正式 repository 在記錄本報告前保持乾淨；產品程式沒有因 smoke 而修改。

## 7. 待使用者實機確認

工程端真實瀏覽器檢查不取代 `Browser Manual Smoke`。正式完成第 15 部分前仍需使用者：

1. 在 Desktop Chrome 人工走一次 Admin 建立帳號、首次登入、我的帳號、雙登入與 Admin Reset。
2. 在真實 iPhone Safari 確認 Password Manager、虛擬鍵盤、直／橫向 Safe Area、
   light／dark mode、強制改密碼頁、Account 與 Sidebar 基本操作。

收到使用者結果後，才可勾選 `PLAN_v1.5.md` 15.1～15.6、將本報告狀態改為通過，並整理
第 15 部分正式交接。
