# v1.3 薪資結算 Smoke 完成交接

日期：2026-07-18
狀態：工程實作、自動驗證、使用者 browser manual smoke 與最終回歸均通過；待建立封板 commit／tag。

## 已完成

- v1.3 `PLAN_v1.3.md` 第 0～12 部分功能與後端測試。
- 第 13 部分 lint、typecheck、production build、角色入口、route guard、格式、狀態與防重送驗證。
- admin-only 員工薪資設定、版本化獎金方案、月份列表／詳情、重算、手動加扣、確認及發薪 UI。
- 40% 公司保留、60% 分配池、收車 20%、賣車 20%／30%／50% 整月跨級。
- approved-only 毛利、完整 eligibility、stale preview、transaction、idempotency、並發與 paid 歷史保護。
- 正毛利合格車的歸屬人未啟用薪資／獎金時，顯示不阻擋確認的提示並說明獎金歸公司剩餘。
- README、API、current-state、smoke report 與本交接文件。
- 第 13.1 節 RWD、dark mode 與操作語意收尾：手機 drawer、卡片／堆疊版面、44px 觸控高度、原生控制項 dark token、精確級距文案與來源確認處理說明。
- 第 13.1 節 review 修正：觸控／表單樣式改為相關頁面 opt-in，不波及全站 compact controls；停用獎金狀態改讀既有 Salary Profiles API，涵蓋 0 台員工。

## 最新證據

```text
Backend full suite：499 tests / 485 passed / 14 skipped / 2293 assertions
MariaDB 10.11.18 專用可拋棄 schemas：14 tests / 176 assertions / 全數通過
Frontend lint：PASS（2 existing warnings）
Frontend typecheck：PASS
Frontend production build：PASS
Firefox 152 + WebDriver BiDi：320／375／390／768／1440px light／dark 全頁無水平 overflow；可見控制項至少 44px
Pint：PASS
git diff --check：PASS
```

MariaDB 測試使用全新可拋棄 schema，完成後已刪除並確認不存在。開發 schema 未清空。

## 部署狀態與邊界

- 本機開發 MySQL 已增量套用 migration `000011`～`000014`，沒有執行 `migrate:fresh`；`000014` 屬獨立 Customer hotfix，詳見 `docs/customer-workflow-hotfix.md`。
- `000013` 實質 forward-only：若升級前已有 confirmed／paid 月份會在 DDL 前 fail-fast；不可用 0 或 items 推測回填。
- 升級前遺留 draft 的公司 totals 為 null，第一次 confirm 會要求先重算，符合安全預期。
- v1.3 不包含打卡、排班、請假、官方勞健保引擎、稅務、薪轉檔、員工薪資單或正式會計。

## Browser Smoke 完成結果

使用者已於 2026-07-18 完成：

1. 1／3／5 台的 20%／30%／50% 整月追溯與單車公式驗算。
2. 底薪、津貼、勞健保、手動加扣、公司分配與全公司實發總額。
3. 未啟用獎金非阻擋提示、pending 收支、購車付款不一致與虧損車。
4. 確認只讀、發薪、paid 狀態保持、兩筆薪資支出與現金餘額下降。
5. manager／sales Sidebar 與 direct route 封鎖。

無法由正常 UI 製造或觀察的缺收／賣車人、同 key replay、薪資 MoneyEntry PATCH／DELETE 及原始 API JSON 邊界，分別由 `SalaryEligibilityTest`、`SalaryPaymentTest`、`SalaryMoneyEntryProtectionTest` 與 `SalaryHttpBoundaryTest` 驗證。

Smoke 過程發現的三類顯示／修正入口缺陷均已修正並由使用者複驗。Smoke 使用的 2026-07 paid 月份、兩筆薪資支出及對應稽核紀錄已在驗收後依使用者授權受控清除，並恢復全部薪資保護 triggers；目前資料庫沒有薪資月份。正式規則已補上當月不得確認，只能建立／重算預估草稿。完整紀錄見 `docs/v1.3-smoke-report.md`。

## 下一步

建立 v1.3 封板 commit，視需要建立 `v1.3-smoke-passed` tag 並 push。未取得使用者明確授權前，不自動執行 Git 寫入操作。
