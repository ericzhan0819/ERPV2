# ERPV2 v1.5 第 13 部分交接

日期：2026-07-27

## 完成範圍

- 完成 `PLAN_v1.5.md` 第 13 部分「Backend Automated Tests」。
- 逐項對照 v1.5 schema／User、Auth、Password Gate、Self Account 與完整回歸要求，確認前面功能階段建立的測試仍直接覆蓋正式契約。
- 新增登入成功必須 regenerate Session ID 的直接回歸測試，並確認既有 Session 資料保留。
- 補強 `Retry-After` 契約，除 header 存在外，另驗證其為 1～60 的正整數秒數。
- 新增未知角色且待改密碼時仍先由 Password Gate 回 `409 + PASSWORD_CHANGE_REQUIRED` 的直接回歸測試。
- 於明確命名的可拋棄 MariaDB schema 實跑 v1.5 migration、大小寫唯一性與真正跨連線 username 競態。
- 本部分只整理與補強自動測試，沒有修改 runtime、API、schema、權限或商業流程。

## 測試契約

### Schema／User

- `username` nullable、unique、正規化與大小寫唯一性。
- `must_change_password` database default 為 `false`，既有使用者不做推測式回填。
- Factory／Seeder 可明確建立預設、待改密碼與具 username 狀態。
- Migration 可 rollback 後重新執行。
- Admin 建立與重設密碼固定設為 `must_change_password=true`，payload 不可覆寫。
- Username stale validation 與真正 database race loser 都轉為欄位 validation error。
- `UserResource` 回傳完整安全欄位，原始 JSON 不含 password／remember token。

### Auth

- Email／username 登入、trim／case normalization、通用錯誤與停用帳號。
- 登入成功 regenerate Session ID、保留 Session 資料並留下 Authentication Audit。
- Username／Email alias 共用 canonical account 與 identifier + IP limiter。
- IP-wide limiter、blocked request 不再查詢／驗證、成功登入清理邊界。
- `429` 保留正整數秒數的 `Retry-After` header。

### Password Gate／Self Account

- 待改密碼狀態只放行 `/api/me`、logout、self profile 與 self password。
- 所有 authenticated 營運路由由 route inventory test 保護，不得漏掛 gate。
- Gate 固定回 `409` 與 `PASSWORD_CHANGE_REQUIRED`，並先於 role、model binding 與 policy。
- 未登入仍回 `401`，停用帳號仍由 active middleware 優先回 `403`。
- 未知角色不能繞過 gate，只能使用既有安全 self account 邊界。
- Admin／manager／sales／未知角色只能修改自己的 name、username 與 password。
- Email、role、active、員工欄位及其他 workflow 欄位只要出現在 self payload 就回 `422`。
- Current password、confirmation、不可沿用原密碼、flag clear、Session regenerate 與 Audit 密碼遮蔽均有直接測試。

## 修改檔案

Backend tests：

- `backend/tests/Feature/DualIdentifierLoginTest.php`
- `backend/tests/Feature/LoginThrottleTest.php`
- `backend/tests/Feature/CurrentUserAccountTest.php`

文件：

- `PLAN_v1.5.md`
- `docs/v1.5-phase13-handoff.md`

## 自動驗證

第 13 部分帳號／權限聚焦基準：

```text
php artisan test \
  tests/Feature/UserAccountSchemaTest.php \
  tests/Feature/UsernameValidationTest.php \
  tests/Unit/UsernameDuplicateExceptionTest.php \
  tests/Feature/DualIdentifierLoginTest.php \
  tests/Feature/LoginThrottleTest.php \
  tests/Feature/CurrentUserAccountTest.php \
  tests/Feature/UserTest.php \
  tests/Feature/AuditLogTest.php \
  tests/Feature/RoleAccessTest.php

160 tests passed，966 assertions
```

新增／補強測試與相鄰契約：

```text
php artisan test \
  tests/Feature/DualIdentifierLoginTest.php \
  tests/Feature/LoginThrottleTest.php \
  tests/Feature/CurrentUserAccountTest.php

58 tests passed，557 assertions
```

完整後端回歸：

```text
php artisan test --log-junit /tmp/erpv2-v15-phase13-junit.xml

624 tests：607 passed，17 environment-gated skipped，3135 assertions
0 errors，0 failures
```

MariaDB 10.11 gated integration：

```text
APP_ENV=testing \
DB_CONNECTION=mysql \
DB_DATABASE=erpv2_v15_phase13_test \
RUN_MYSQL_CONCURRENCY_TESTS=1 \
MYSQL_CONCURRENCY_TEST_CONNECTION=mysql \
MYSQL_CONCURRENCY_TEST_DATABASE=erpv2_v15_phase13_test \
php artisan test --filter=UserAccountMysqlIntegrationTest

2 tests passed，36 assertions
```

格式：

```text
vendor/bin/pint --test \
  tests/Feature/DualIdentifierLoginTest.php \
  tests/Feature/LoginThrottleTest.php \
  tests/Feature/CurrentUserAccountTest.php

passed
```

## Database Safety

- MariaDB 驗證使用全新的可拋棄 schema `erpv2_v15_phase13_test`。
- 建立前先確認 schema 不存在；測試只將該 schema 暫時授權給既有測試連線使用者。
- 測試完成後撤銷該 schema 權限並刪除 schema，再由 `INFORMATION_SCHEMA` 確認剩餘數量為 0。
- 未執行或修改既有 `erpv2` 開發資料庫。

## 未執行

- Frontend test／lint／typecheck／build：第 13 部分只修改 Backend tests 與文件；Frontend Automated Tests 屬第 14 部分。
- Browser manual smoke：完整 v1.5 人工驗收屬第 15 部分，不以自動測試冒充。

## 風險與下一部分

- 完整回歸的 17 個 skip 為既有 MariaDB／環境 gated tests；本部分直接相關的 `UserAccountMysqlIntegrationTest` 已在可拋棄 MariaDB schema 全數通過。
- 測試確認既有 runtime 契約，沒有為了測試階段擴張帳號、權限或 Settings 功能。
- 下一部分依 `PLAN_v1.5.md` 第 14 部分整理 Frontend Automated Tests。

## 建議 Commit Message

```text
test：完成 v1.5 後端自動回歸
```
