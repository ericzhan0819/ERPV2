# v1.3 薪資結算 Smoke Report

日期：2026-07-18
狀態：使用者 Browser Smoke 與最終自動回歸皆通過；待建立封板 commit／tag。

## 1. 自動驗證結果

### Backend full suite

```text
php artisan test
499 tests
485 passed
14 skipped（未啟用專用 MySQL schema 的環境條件測試）
2293 assertions
```

### 真實 MariaDB 10.11

在名稱 allowlist 明確指定的全新可拋棄 schemas 執行：

```text
VehicleFinalPaymentMysqlConcurrencyTest
VehicleCreateMysqlConcurrencyTest
SalarySettingsMysqlConcurrencyTest
TimezoneMysqlIntegrationTest

14 tests passed
176 assertions
```

並發測試與時區整合測試因各自使用獨立 allowlist guard，分別在專用 schema 執行；其中 Customer Workflow hotfix 新增並發建車共用一位新賣方、並發保留共用一位新買方。測試完成後均刪除 schema，並查詢 `INFORMATION_SCHEMA` 確認剩餘數量為 0。既有 `erpv2` 開發 schema 未執行 `migrate:fresh` 或 `db:wipe`。Customer 詳細結果見 `docs/customer-workflow-hotfix.md`。

### Frontend

```text
npm run lint     PASS（0 errors；2 個既有 Fast Refresh warnings）
npx tsc -b       PASS
npm run build    PASS
```

### 其他

```text
Pint targeted check   PASS
git diff --check      PASS
```

### 第 13.1 節前端瀏覽器驗證

```text
Firefox 152 headless + WebDriver BiDi
viewport：320 / 375 / 390 / 768 / 1440px
theme：light / dark
結果：薪資月份、月份詳情、薪資設定、獎金方案、待補歸屬皆無整頁水平 overflow
控制項：可見導覽、button、input、select、textarea 均至少 44px
```

另已實際操作手機 drawer 開關與換頁自動收合、薪資明細展開、手動加扣 modal、薪資設定編輯及獎金方案表單；檢查 dark mode 原生 select／option 的 foreground、surface 與 border token。這是第 13.1 節工程驗證，不取代下列使用者以完整業務資料執行的第 14 節 manual smoke。

對立性審查後已將 44px 與表單色彩規則收斂為 `.form-control-touch` opt-in，避免改變其他模組的 compact buttons；原受影響 class 的瀏覽器高度回歸為 16px／26px／38px。另改由 Salary Profiles API 判斷草稿員工的 `commission_enabled`，涵蓋停用且 0 台情境。修正後 50 組 viewport／theme／頁面回歸為 0 failure。

## 2. 自動驗證涵蓋

- 正式收車人／賣車人欄位與 idempotency payload。
- 版本化獎金方案、1／3／5 台整月跨級與歷史不可回改。
- approved-only 毛利、pending／rejected 排除、虧損與整數餘數。
- 底薪、津貼、勞健保、手動加扣、員工及全公司 totals。
- 公司保留與公司剩餘直接保存 calculator 的整月批次 totals。
- 無 active salary profile／未啟用獎金時的非阻擋提示與公司剩餘歸屬。
- stale preview、確認鎖定、paid DB trigger、發薪 transaction 與 idempotency race。
- admin-only API／Resource 白名單及 manager／sales 原始 JSON 遮蔽。
- 前端 admin-only Sidebar／route guard、集中 API、錯誤與空狀態、action-level 重複送出保護。

## 3. Browser Manual Smoke 結果

測試日期：2026-07-18

執行者：使用者

結果：通過

### 顯示與裝置

- [x] 手機寬度可基本查看與操作。
- [x] light／dark mode 文字、badge、表格、modal 與警告皆可讀。

### 前置資料

- [x] 建立至少兩位員工薪資設定。
- [x] 建立／確認 2026 標準薪資方案。
- [x] 建立至少五台同一賣車人、同月份 sold 車輛。
- [x] 至少一台收車人與賣車人不同，至少一台同人收車又賣車。
- [x] 每台 approved 購車付款與 purchase_price 一致，approved 收款達 sold_price。

### 跨級與公式

- [x] 1～2 台為 20%，第 3 台後整月 30%，第 5 台後整月五台全部 50%。
- [x] 100,000 毛利顯示公司保留 40,000、收車獎金 12,000、50% 賣車獎金 30,000、公司剩餘 18,000。
- [x] 公司保留＋所有獎金＋公司剩餘＝毛利。
- [x] 同一人收車又賣車時同時取得兩項獎金。
- [x] 未啟用獎金時顯示非阻擋提示，相應獎金歸公司剩餘；重新啟用並重算後恢復。

無 active salary profile 的同類非阻擋情境由 `SalaryHttpBoundaryTest` 專用資料驗證，未冒充 browser 操作。

### 薪資與異常

- [x] 底薪、固定津貼、勞保、健保、手動加給、手動扣款、實發薪資與全公司總額正確。
- [x] pending 收支與購車付款不一致皆由 browser 實際製造，成功阻擋確認；修正後恢復可確認。
- [x] 虧損車不產生負獎金，且不推升賣車級距。

缺收車人／缺賣車人無法由正常 UI 流程製造，因建車與成交流程已強制指定；由 `SalaryEligibilityTest` 驗證兩種歷史異常會列出 stock number、修正 action 並阻擋確認。

### 確認與發薪

- [x] 確認後只讀，重新整理後狀態保持。
- [x] 發薪為每位正數實發員工建立一筆 MoneyEntry，帳戶餘額正確下降。
- [x] 2026-07 實際建立兩筆 approved `salary_settlement` 支出：系統管理員 86,500、Sales 238,000。
- [x] 現金餘額由 545,000 降至 220,500。
- [x] 重新整理後 paid 狀態、發薪日期與帳戶保持。

收支清單本來就沒有一般編輯／刪除 UI，因此不能以按鈕不存在證明薪資支出特別保護；`SalaryPaymentTest` 與 `SalaryMoneyEntryProtectionTest` 已驗證同 payload replay 不重複建立、衝突 payload 拒絕、一般 PATCH／DELETE 回 422，以及資料庫層不可改寫／刪除。

上述 2026-07 paid 月份與兩筆薪資支出是 Browser Smoke 驗收資料。驗收完成後，使用者發現月份尚未結束卻已鎖定會阻擋後續 7 月成交；依使用者明確授權，以受控方式清除該月份、薪資支出與對應稽核紀錄，並完整恢復薪資保護 triggers。這不撤銷本節的歷史驗收結果。正式規則同步改為「當月可建立／重算草稿，但月份結束後才能確認」。

### 權限

- [x] admin 可完整操作。
- [x] manager 無 Sidebar 入口，直接輸入 `/salary`、月份詳情、薪資設定與獎金方案路徑均導回 Dashboard。
- [x] sales 無 Sidebar 入口，直接路由同樣被導回 Dashboard。
- [x] `SalaryHttpBoundaryTest` 驗證 manager／sales 對每個薪資月份 endpoint 固定回 403，原始 JSON 白名單不洩漏薪資欄位。
- [x] `SalaryMoneyEntryProtectionTest` 驗證 manager／sales 不能列舉或讀取薪資支出內容。

## 4. Smoke 過程發現並修正的缺陷

1. 稽核紀錄缺少 v1.3 資料類型中文映射，已補上薪資設定、獎金方案、薪資月份與薪資加扣項。
2. 薪資明細勞保／健保項目顯示原始 type，已修正中文標籤映射。
3. 已售車缺少收購價時，薪資會阻擋但前端沒有修正入口；已新增 admin「補登／修正收購價」，並在 confirmed／paid 月份由後端鎖定禁止回改。
4. 缺收購價異常的修正文案改為「補登收購價」，不再錯誤引導只查看購車付款。

以上修正均完成對應回歸測試、frontend lint／typecheck／build，並由使用者重新驗證畫面。

## 5. 封板狀態

- [x] `PLAN_v1.3.md` 第 14 部分與第 17 部分 manual smoke 已勾選。
- [x] Browser Smoke 通過。
- [x] 最終 backend full suite 通過：485 passed、14 environment-gated skipped、2293 assertions；所有受保護 MariaDB 並發／時區測試於專用 schemas 共 14 passed、176 assertions。
- [x] frontend lint／typecheck／production build 通過。
- [ ] 建立封板 commit／tag。

目前功能與驗收均完成；因本次未取得使用者 commit／push／tag 授權，文件狀態記為「Smoke 通過，待建立封板 commit／tag」，不宣稱 Git 封板已完成。
