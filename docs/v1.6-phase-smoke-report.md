# ERPV2 v1.6 工程 Browser Smoke Report

日期：2026-07-29

範圍：`PLAN_v1.6.md` 第 13 章

狀態：工程 smoke 通過；尚未取代第 15 章使用者 manual smoke

## 環境與資料

- Frontend：Vite dev server，`http://127.0.0.1:5196`
- Backend：真實 Laravel API，`http://127.0.0.1:8016`
- Database：專用 `/tmp/erpv2-v16-smoke-final.sqlite`
- Browser：Google Chrome for Testing `151.0.7922.47`
- Driver：ChromeDriver `151.0.7922.47`
- Screen reader：Orca `46.1`，以 Xvfb、非 headless Chrome 與 `--force-renderer-accessibility` 執行
- Viewport：1440×1000、768×1024、390×844、375×667、320×568
- Theme：light、dark
- 角色：admin、manager、sales

專用 fixture 由 `docs/smoke/v1.6-phase13-fixtures.php` 建立；腳本只允許
`APP_ENV=testing`、SQLite 且資料庫路徑符合
`/tmp/erpv2-v16-smoke-*.sqlite`。資料涵蓋：

- Dashboard 有值與零值、pending／rejected／approved 收支。
- preparing／listed／reserved／sold／cancelled 車輛狀態。
- admin／manager／sales 三角色。
- draft／confirmed／paid 薪資月份。
- 車輛成本、成交價、收款、approved-only 毛利與資金帳戶。

測試完成後已停止專用服務，並刪除專用 SQLite、Chrome、Orca prefs／debug
與其他 `/tmp/erpv2-v16-*` 暫存檔；既有開發資料與服務未變更。

## Chrome 工程操作結果

`docs/smoke/v1.6-phase13-webdriver.py` 透過 WebDriver 操作真實 UI 與 API。
正式精確 Mobile viewport 證據使用 Chrome CDP device metrics，並逐次驗證
`window.innerWidth`。

結果：32 checks passed。

- 1440px light／dark：Dashboard、Account、User、Vehicle、MoneyEntry、
  Customer、Salary、Audit、Cash Account 均能載入，沒有 document overflow。
- Keyboard-only：由 `body` 起始，以 Tab 抵達「我的帳號」，按 Enter 完成導頁。
- 狀態：Account success、draft salary warning、form error、Vehicle filtered
  empty state 均可感知；confirmed／paid 薪資後果仍完整。
- 高風險規則：approved-only、成交月份、收款日期、薪資鎖定／發薪、
  期末餘額與 Sales 敏感金額遮蔽仍可理解。
- Print：車輛建檔與成交結案兩份真實 WebDriver PDF 均以 `%PDF` 開頭，
  檔案大小皆大於 10 kB。
- 三角色：admin 可見完整營運入口；manager 可見財務 KPI 且無
  admin-only 入口；sales 不見財務 KPI、成本與毛利，但銷售收款仍可理解。
- 768／390／375／320px light／dark：Dashboard、Vehicle、MoneyEntry、
  Customer、User、Cash Account、draft Salary 均無整頁水平 overflow；
  Header／action、已刪副標空間與條件式 warning 未造成版面回歸。
- 320px：首錯可見且取得焦點，label／error 未超出 viewport；
  Filter Drawer 與成交 Modal 可用 Escape 關閉；員工／資金帳戶表格可在
  自己的容器橫向捲動；self 限制短句與 Safe Area 未破版。

首次正式重跑曾在服務剛啟動時發生一次登入導頁逾時；直接 CSRF 登入與隔離
WebDriver 均為 200，後續完整重跑通過。另兩次擴充跑因命令工作時間窗在
375px 前終止，未列為通過證據；最終以持續 PTY 工作階段完成 32/32。

## 真實 screen reader 結果

`docs/smoke/v1.6-phase13-orca.py` 以 Orca 真實 speech output 抽查：

```text
SR_MARKER login_alert {'focused': True, 'live': 'off', 'text': '帳號或密碼錯誤'}
SR_MARKER field_error {'errorText': '請選擇分類', 'focused': True, 'name': '分類'}
SR_MARKER sibling_label {'ariaLabel': '', 'labels': ['帳戶名稱'], 'tag': 'INPUT'}
login_speech_output_count=1
field_speech_output_count=1
label_speech_output_count=1
```

- 主動聚焦的登入 general error 保留 `role="alert"`，但關閉額外 live
  announcement，Orca 只朗讀一次。
- 新增收支第一個錯誤欄位在 AT-SPI 關聯穩定後取得焦點，分類名稱與
  「請選擇分類」各只朗讀一次。
- 資金帳戶建立／編輯欄位補上原生 `label[for]`／`id` 關聯；Orca 可朗讀
  「帳戶名稱 entry required」。

## Browser finding 與修正

1. Vehicle workflow Modal 與 Salary adjustment Modal 缺少 dialog semantics、
   Escape、Tab containment、body scroll lock 與 trigger focus return。
   新增共用 `useDialogFocus`，並補 component tests。
2. `FormAlert` 同時使用 assertive live region 與主動 focus，在 Orca 會重複
   朗讀。主動 focus 模式改為 `aria-live="off"`；被動錯誤仍維持 assertive。
3. 資金帳戶 sibling visible labels 沒有程式化關聯。建立／編輯欄位補上
   `htmlFor`／`id`，並新增 accessible-name 回歸測試。
4. MoneyEntry 首錯立即 focus 時，Orca 會重複朗讀 description。延後 100ms
   聚焦，讓 AT-SPI 關聯先穩定；Vitest 與真實 Orca 均通過。

以上只調整前端 Accessibility／Presentation 與對應測試；沒有 Backend、
API、schema、Business Logic、Workflow、權限或 dependency 變更。

## 自動回歸

```text
cd frontend && npm test
cd frontend && npm run lint
cd frontend && npm run typecheck
cd frontend && npm run build
git diff --check
```

- Vitest：27 files／148 tests passed。
- lint：0 errors；2 個既有 Fast Refresh warnings。
- typecheck：通過。
- production build：通過；1902 modules，JS 528.31 kB／gzip 141.28 kB，
  CSS 33.59 kB／gzip 7.32 kB；保留既有 >500 kB chunk warning。
- `git diff --check`：通過。

截圖不是本章通過依據；證據來自實際 navigation、keyboard、focus、
responsive metrics、API-backed state、PDF 產生與 Orca speech output。
