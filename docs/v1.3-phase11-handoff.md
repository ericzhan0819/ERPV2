# v1.3 第 11 部分交接

日期：2026-07-15  
狀態：薪資管理前端已完成自動驗證；完整人工 Smoke 尚未執行。

## 本階段完成

- 新增集中式 salary profiles、commission plans、salary periods API client 與 TypeScript types。
- 新增 admin-only 薪資月份列表、月份詳情、員工薪資設定及獎金方案頁面。
- 月份詳情支援草稿重算、異常修正入口、手動加扣、確認、選帳戶與日期發薪，以及車輛獎金明細。
- Sidebar、SPA route guard 與 Dashboard 本月預估薪資僅對 admin 開放；manager／sales 不呼叫薪資 API。
- 公司營運保留與公司剩餘分配額由後端 calculator 依整月 eligible vehicles 計算並保存到月份，不受員工 settlement 是否存在影響，也不由 React 重算。
- admin 車輛詳情會顯示 confirmed／paid 薪資月份造成的獎金歸屬鎖定原因與月份連結。
- `PLAN_v1.3.md` 第 11 部分已依實際完成項目勾選。

## 自動驗證

```text
cd frontend && npm run build
通過：TypeScript + Vite production build

cd frontend && npm run lint
通過：0 errors；2 個既有 useAuth/useTheme Fast Refresh warnings

cd backend && php artisan test
通過：480 tests，468 passed，12 skipped，2205 assertions

cd backend && vendor/bin/pint --test <本次 PHP 修改檔案>
通過

git diff --check
通過
```

12 個 skipped 為既有環境條件測試；本次未把 skipped 宣稱為通過。

## 人工 Smoke 重點

1. admin 可看到「薪資結算」，manager／sales 看不到，直接輸入路由會回總覽。
2. 建立當月草稿，確認空狀態、loading、錯誤訊息與月份列表金額。
3. 逐一檢查員工薪資欄位、成交台數、級距、下一級距、車輛獎金及公司摘要。
4. 製造缺歸屬或 pending 收支，確認異常會阻擋確認且修正入口可到正確頁面。
5. 新增及刪除其他加給／扣款，重算後確認手動項目仍保留。
6. 確認結算後頁面只讀；選啟用帳戶與合法日期發薪，確認二次提示與完成狀態。
7. 開啟已納入 confirmed／paid 月份的車輛，確認顯示歸屬鎖定原因與月份連結。
8. 在 light／dark mode 與窄螢幕確認表格可橫向捲動、表單可操作。

## 後續邊界

- 下一步是 `PLAN_v1.3.md` 第 13 部分完整 Smoke／回歸與封版文件。
- 未執行瀏覽器人工操作或真實 MariaDB smoke；需由使用者在目標環境驗收。
- 部署新增 migration 前若資料庫已有 confirmed／paid 薪資月份會 fail-fast；不可直接刪資料或改填 0，需先人工評估歷史資料。既有 draft 升級後應執行一次重算。
- 不要把打卡、排班、請假、官方勞健保級距、所得稅或薪轉檔混入 v1.3。
