# ERPV2 v1.4 完成交接

日期：2026-07-23
狀態：`PLAN_v1.4.md` 第 0～14 部分、工程驗證、Browser Manual Smoke、文件同步與封板前 follow-up 均已完成，並以 `v1.4-smoke-passed` tag 封板；tag 指向 `d4ea978`，runtime 與 follow-up 基準為 `a10dd0c`。

## 1. 完成內容

- Dashboard 固定為 Action Bar、工作概況、經營概況、趨勢分析四區塊；KPI 只以 URL Filter 導向既有模組。
- Dashboard 後端提供正式工作 KPI、經營 KPI 與 30 天趨勢；財務資料只計 approved，sales／未知角色原始 JSON 不含財務欄位。
- Vehicle 工作區改為 Card Grid，Mobile 1 欄、Tablet 2 欄、Desktop 多欄；只讀取封面縮圖，維持角色價格與敏感資料遮蔽。
- Vehicle／MoneyEntry Filter 由 URL 保存，支援 reload、上一頁／下一頁、分頁與 Mobile Drawer；`sold_month` 使用台北月份半開區間。
- App Shell、Mobile Sidebar、light／dark mode 與 iPhone Safari Safe Area 完成收尾，沒有新增 Header 快捷功能、通知或 Footer。
- `UI.md`、`backend/API.md`、`README.md`、`AGENTS.md`、`CLAUDE.md`、`docs/current-state.md`、Smoke Report 與 PLAN 已同步。
- 封板前 follow-up 已修正 CustomerSelect 空白搜尋後編輯卡住狀態，以及 MoneyEntry 載入期間短暫顯示過期分頁資訊。

## 2. 最終驗證證據

```text
Backend full regression：519 tests；504 passed、15 environment-gated skipped、2516 assertions
MariaDB 10.11 專項（第 10 部分）：3 tests、35 assertions，全數通過並刪除可拋棄 schema
Frontend Vitest：3 files、14 tests，全數通過
Frontend lint：0 errors；2 個既有 Fast Refresh warnings
Frontend typecheck：PASS
Frontend production build：PASS；1890 modules
Firefox Browser Smoke：admin／manager／sales、導流、Filter、五種寬度、light／dark、鍵盤與 Drawer 通過
使用者真實手機：中文輸入法與 iPhone Safari Sidebar／Safe Area／主題切換通過
```

本次第 12～14 部分重新執行 backend full regression 與全部 frontend 指令；真實 MariaDB 未重跑，因 v1.4 最終收尾只有文件且沒有 schema、transaction 或並發邏輯變更，沿用第 10 部分的專用 schema 證據。Production build 保留既有 >500 kB chunk warning，沒有新增 runtime 圖表 dependency。

## 3. 範圍與相容性

- `v1.3-smoke-passed..HEAD` 沒有 migration 或 schema 差異。
- 沒有新增或變更車輛／收支／客戶／薪資 Workflow、狀態、角色、權限或財務公式。
- Dashboard 不承載 Table、車輛列表、展開明細、薪資 KPI、通知或未指定報表。
- 回報整備支出重用 `/vehicles?status=preparing` 與既有車輛詳情流程。
- Vehicle List 不回傳完整相簿、收購價、毛利、資金帳戶或收支摘要；後端 Resource 仍是正式安全邊界。
- 未加入任意成交日期區間、跨月比較、月份報表、排行或匯出。

## 4. 部署與後續 Smoke 重點

部署後優先複查 Dashboard 三角色原始輸出、KPI URL、Vehicle 預設狀態、`sold_month`、Mobile Drawer、iPhone Sidebar 開關與 light／dark Safe Area。若更換時區、資料庫版本或反向代理設定，再重跑 `TimezoneMysqlIntegrationTest` 的專用 schema 測試。

Runtime 與封板前修正已 commit、push 至 `a10dd0c`；最終文件校正 commit 為 `d4ea978`。使用者已於 2026-07-23 明確授權建立 `v1.4-smoke-passed` annotated tag，並已推送至 `origin`；tag 解引用後指向 `d4ea978`。

## 5. 封板前 follow-up

- `9289f5f fix：修正客戶搜尋空白編輯卡住狀態`：CustomerSelect 的搜尋 effect 同時依賴原始姓名與 trim 後查詢字，讓只改變前後空白的輸入也能重新收斂 debounce 與 loading 狀態；送往 API 的查詢仍使用 trim 後內容，修正已記錄於 `docs/v1.4-phase11-handoff.md`。
- `a10dd0c fix：修正收支列表載入期間過期分頁資訊`：MoneyEntry 列表在 Filter 切換或核准／駁回重新查詢開始時清除舊分頁 `meta`，避免 loading 期間短暫顯示上一筆回應的頁數與總筆數；既有 request sequence、Filter URL、審核流程與資料可見性不變。

2026-07-23 對立性審查未發現 critical／high／medium 缺陷。最終 MoneyEntry 修正後 frontend Vitest 3 files／14 tests、lint（0 errors；2 個既有 Fast Refresh warnings）、typecheck 與 production build 均通過。
