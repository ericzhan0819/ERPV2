# ERPV2 v1.5 第 5 部分交接

日期：2026-07-25

## 完成範圍

- 完成 `PLAN_v1.5.md` 第 5 部分「首次登入與重設密碼狀態」。
- Admin 建立員工時，Service 固定建立 `username=null`、`must_change_password=true`。
- Admin 重設密碼時，在同一個 transaction 內更新密碼、設為 `must_change_password=true`，並留下不含密碼與 hash 的 User Audit。
- Store／Reset Request 明確拒絕由 payload 指定帳號名稱或覆寫強制改密碼狀態。
- 使用者管理列表會標示「需修改密碼」，重設成功後重新讀取列表。
- 未提前實作第 6 部分強制改密碼 Middleware、自助改密碼端點或前端強制導流。

## 正式契約

### Admin 建立員工

`POST /api/users` 不接受 `username` 或 `must_change_password`。即使直接呼叫 `UserService::createUser()`，服務仍明確覆寫為：

```text
username = null
must_change_password = true
```

回應沿用 `UserResource`，會回傳上述狀態，但不會回傳 password 或 hash。

### Admin 重設密碼

`POST /api/users/{id}/reset-password` 只接受新密碼；任何 `must_change_password` 鍵值都回 422。成功時：

```text
password = 新密碼 hash
must_change_password = true
```

密碼與 flag 使用同一次 `save()`，並包在同一個 database transaction。若 Audit Observer 無法寫入稽核紀錄，整個重設會 rollback，舊密碼與舊 flag 都保留。

既有 Session 在下一個請求重新解析 User 時會讀到新的 `must_change_password=true`。第 6 部分才會以 Middleware 將此狀態轉為一般營運 API 的 409 阻擋。

### 既有帳號

- v1.5 migration 的 database default 仍為 `false`。
- 不依 `created_at`、Email、角色或其他既有資料推測誰需要改密碼。
- `AdminUserSeeder` 仍明確維持 `must_change_password=false`，不會把開發管理員鎖住。

## 修改檔案

Backend：

- `backend/app/Http/Requests/StoreUserRequest.php`
- `backend/app/Http/Requests/ResetUserPasswordRequest.php`
- `backend/app/Services/UserService.php`
- `backend/tests/Feature/UserTest.php`
- `backend/tests/Feature/AuditLogTest.php`
- `backend/API.md`

Frontend：

- `frontend/src/types/user.ts`
- `frontend/src/pages/users/UserList.tsx`

文件：

- `PLAN_v1.5.md`
- `docs/v1.5-phase5-handoff.md`

## 自動驗證

後端專項：

```text
php artisan test --filter='UserTest|AuditLogTest'
50 tests passed，211 assertions
```

後端格式：

```text
vendor/bin/pint --test app/Http/Requests/StoreUserRequest.php app/Http/Requests/ResetUserPasswordRequest.php app/Services/UserService.php tests/Feature/UserTest.php tests/Feature/AuditLogTest.php
passed
```

完整後端回歸：

```text
php artisan test --log-junit /tmp/erpv2-v15-phase5-junit.xml
588 tests：571 passed，17 environment-gated skipped，2845 assertions
0 errors，0 failures
```

前端：

```text
npm test
3 files／14 tests passed

npx tsc -b --noEmit
passed

npm run lint
passed（既有 useAuth／useTheme Fast Refresh warnings 各 1，無 error）

npm run build
passed（既有單一 bundle 超過 500 kB warning）
```

## 未執行

- MariaDB gated integration：本部分沒有 migration、並發寫入或資料庫方言差異；完整後端回歸中的 17 項環境 gated 測試照既有條件跳過。
- Browser manual smoke：本輪只有既有使用者列表增加狀態 badge 與重設後刷新，未啟動 browser 環境。

## 下一部分

第 6 部分應新增 Password Change Required Middleware，並只放行 `/api/me`、登出、個人資料與個人密碼端點。不得把第 5 部分的 Admin 建立／重設狀態邏輯複製到 Middleware，也不得修改第 4 部分 canonical Login Rate Limiter。
