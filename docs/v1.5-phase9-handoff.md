# ERPV2 v1.5 第 9 部分交接

日期：2026-07-26

## 完成範圍

- 完成 `PLAN_v1.5.md` 第 9 部分「Frontend Auth Context 與 Route Gate」。
- Auth Context 已可在 self profile／password API 成功後安全更新目前使用者，後續「我的帳號」頁可直接使用。
- Protected Route 已統一處理未登入、待改密碼、角色限制與既有 logout pending／blocked 狀態。
- API client 已集中辨識 `409 + PASSWORD_CHANGE_REQUIRED`，不需在各業務頁重複判斷。
- 第 10 部分的 Login 欄位調整與強制修改密碼表單尚未提前實作。

## 正式契約

- Login 與 `/api/me` 取得的完整 `UserResource` 都直接進入 Auth Context，包含 `username` 與 `must_change_password`。
- Auth Context 提供 `updateProfile()`、`updatePassword()`；成功回應只可更新同一個仍在登入中的 User。
- 登出 pending／blocked、已登出或不同 User 的過期 self API 回應，不得恢復或覆蓋 Context。
- `must_change_password=true` 的一般 protected route 固定導向 `/change-password`。
- 強制頁於第 10 部分掛載時須使用 `passwordChangeOnly` route gate：flag=true 才可顯示，flag=false 回 Dashboard，因此不會形成 redirect loop。
- Route 判斷順序固定為 loading → logout pending／blocked → authentication → password-change gate → role。
- API response interceptor 只有在 HTTP 409 且 response `code` 精確等於 `PASSWORD_CHANGE_REQUIRED` 時通知 Auth Context；其他一般 409 維持原頁面既有錯誤處理。
- 收到 password-required 通知時只把目前登入者同步為待改密碼，不改動 logout marker；登出安全狀態永遠優先。

## 修改檔案

Frontend：

- `frontend/src/api/client.ts`
- `frontend/src/auth/currentUserState.ts`
- `frontend/src/auth/currentUserState.test.ts`
- `frontend/src/auth/passwordChangeRequired.ts`
- `frontend/src/auth/passwordChangeRequired.test.ts`
- `frontend/src/auth/protectedRouteDecision.ts`
- `frontend/src/auth/protectedRouteDecision.test.ts`
- `frontend/src/auth/sessionState.ts`
- `frontend/src/hooks/useAuth.tsx`
- `frontend/src/routes/ProtectedRoute.tsx`

文件：

- `PLAN_v1.5.md`
- `docs/v1.5-phase9-handoff.md`

## 驗證

```text
cd frontend && npm test
7 files／28 tests passed

cd frontend && npm run lint
0 errors；2 個既有 Fast Refresh warnings

cd frontend && npx tsc -b --noEmit
passed

cd frontend && npm run build
passed；只有既有 chunk size warning

git diff --check
passed
```

測試直接涵蓋：

- 未登入導 Login。
- Login／me 帶回待改密碼 flag 時，營運 route 導向強制頁。
- 強制頁 route gate 不循環導向，完成改密碼後回 Dashboard。
- 待改密碼判斷先於 role gate。
- logout pending／blocked 先於 auth redirect。
- 只有精確的 `409 + PASSWORD_CHANGE_REQUIRED` 會通知 Auth Context。
- self API 完整回應只更新同一個 idle session；登出中、已登出或不同 User 回應被拒絕。

## 未執行

- Backend tests：本部分沒有後端程式、API、migration、transaction 或權限變更；沿用第 8 部分完整 backend regression。
- Browser manual smoke：第 10 部分強制修改密碼頁與第 11 部分「我的帳號」頁尚未完成，目前沒有完整可操作 UI。
- MariaDB gated integration：本部分沒有 database 變更。

## 部署與下一部分

第 9 部分仍是不可單獨部署的工程中間狀態。Route Gate 已正式導向 `/change-password`，但該 route 與實際表單依 PLAN 屬於第 10 部分；在第 10 部分完成前，待改密碼使用者會被正確隔離於營運頁，但尚無前端表單可解除狀態。

下一部分依 `PLAN_v1.5.md` 第 10 部分：

- Login 改為「帳號名稱或 Email」並依 flag 導向。
- 建立 `/change-password` 獨立 route 與表單。
- 該 route 使用本部分的 `passwordChangeOnly` gate，不掛 AppLayout／Sidebar。
- 成功後呼叫 Auth Context `updatePassword()`，取得 flag=false 的完整 User，並導向 Dashboard。
