# ERPV2 v1.5 Smoke Report

日期：2026-07-29

狀態：v1.5 工程實作、自動回歸、MariaDB 專用整合、Firefox／Desktop Chrome 工程預驗證、使用者 Desktop Chrome／Mobile Safari Browser Manual Smoke 與第 16 部分最終安全審查均已通過。尚未建立 annotated tag。

## 1. 完成範圍

- nullable unique username、`must_change_password` 與安全 migration。
- username／Email 雙登入與 alias-safe Rate Limiter。
- 新建員工／admin reset 後首次登入強制改密碼。
- 後端 `409 PASSWORD_CHANGE_REQUIRED` fail-closed gate。
- 我的帳號 name／username／password 自助管理。
- Admin User List 的 username 與待改密碼狀態。
- Login、Sidebar 與 Browser title 的前端 app config。
- v1.1～v1.4 既有角色、權限、Theme 與主要流程回歸。

## 2. 自動與資料庫驗證

```text
cd backend && php artisan test
633 tests；616 passed、17 environment-gated skipped、3170 assertions

cd frontend && npm test
16 files／80 tests passed

cd frontend && npm run lint
passed；0 errors、2 個既有 Fast Refresh warnings

cd frontend && npm run typecheck
passed

cd frontend && npm run build
passed；保留既有大於 500 kB chunk warning
```

MariaDB 10.11 專用可拋棄 `erpv2_v15_test`：

```text
UserAccountMysqlIntegrationTest
2 tests／36 assertions passed
```

該測試驗證 case-insensitive username unique、migration rollback order，以及兩個真實連線搶同一 username 時 loser 轉為 validation error。測後 schema 與 database-level grant 均已清除，既有 `erpv2` 資料未重建。

17 個 full suite skipped tests 都有 environment gate；其中 v1.5 的兩個 MySQL-only username tests 已另外於 MariaDB 通過，其餘屬既有薪資、時區與車輛並發情境，本輪未重跑。

## 3. Browser 驗證

工程預驗證：

```text
Firefox 152 headless：38 checks passed
Chrome for Testing 151.0.7922.47：15 checks passed
App config 暫改：2 checks passed
```

使用者於 2026-07-29 完成 `PLAN_v1.5.md` 15.1～15.6 共 33 個正式項目：

- Admin 建立員工、Email 首次登入、強制改密碼與 Dashboard／API gate。
- 我的帳號、username 唯一性、名稱即時更新與密碼欄位錯誤。
- username／Email／大小寫登入與通用錯誤訊息。
- Admin reset、兩個既有 stateful Session 各自於下次操作回 401、重新登入與再次強制改密碼。
- App config、Theme、admin／manager／sales 主要流程。
- Desktop Chrome 與 Mobile Safari 基本操作。

詳細 driver、逐項對照、環境及清理證據見 `docs/v1.5-phase15-smoke-report.md` 與 `docs/smoke/README-v1.5-phase15.md`。

## 4. 第 16 部分最終審查與 Claude follow-up

- Password：Request 只將密碼傳入 Auth／Hash 流程；`UserResource` 不含密碼；User model hidden；Audit model event 會剔除 `password`，authentication audit 不保存原始 login；未發現將密碼值寫入一般 log 的程式。
- Alias limiter：可解析的 username／Email 都以 `uid:<id>` 作為 canonical identity，共用 account 與 identifier + IP limiter；正反順序、大小寫、成功清除與不存在識別均有回歸測試。
- Middleware：authenticated 營運 routes 預設套用 `active` 與 `password.changed`；只有 `/api/me`、self profile、self password 明確排除；route coverage test 防止新增路由漏掛。
- Self endpoint：profile／password FormRequest 對 Email、role、is_admin、is_active 與其他管理欄位採 `missing` 拒絕，不會靜默忽略。
- App config：只有 source-controlled `frontend/src/config/app.ts`，沒有 settings table、API 或線上設定頁。
- 範圍：第 16 部分文件同步本身未新增 schema、dependency 或無關 UI 重構，未修改 v1.1～v1.4 PLAN。

初次 Codex 審查未發現阻擋問題。後續 Claude adversarial review 判定整體通過，另提出
4 個低嚴重度項目；Codex 回到實際程式與測試查證後處理如下：

1. 有效：停用帳號輸入正確密碼時，原本會在 active 檢查前清除 identifier + IP 與
   canonical account limiter。修正為只有 active 正式登入才清除；停用分支保留已記入的
   兩層嘗試並增加 IP-wide failure。回歸測試確認第五次停用帳號正確密碼嘗試後三層計數
   皆保留，下一次 alias 嘗試回 `429`。
2. 有效：`409 PASSWORD_CHANGE_REQUIRED` event 原本未攜帶 request generation。修正後每個
   409 從 Axios config 取出 stamped generation，Context 只接受與目前 generation 相同的
   通知；未追蹤或舊 Session 回應不再把新登入者導向強制頁。
3. 有效：self profile 已在後端提交、但前端 Auth Context 已切換時，原本會落入通用失敗
   訊息。修正為辨識 committed-stale response，導向 Login 並顯示「個人資料已更新，請
   重新登入確認」，不再誤報資料庫寫入失敗。
4. 不在 v1.5 規格範圍：`PATCH /api/me/password` 額外 per-user throttle。此項是新的 429
   門檻、key 與成功請求計數契約，不是既有實作違反；未經產品決策不直接擴張。若後續要
   採用，應先在新 hotfix PLAN 明確定義門檻、回應與測試。

Claude 另實測 Laravel 13 `SessionGuard::attempt()` 的 Timebox，存在與不存在帳號皆約
200ms，否定登入 timing oracle 疑慮，因此不列 finding。

修正後直接驗證：

```text
LoginThrottleTest + DualIdentifierLoginTest：28 tests／310 assertions
Frontend 相關 6 files：32 tests
Backend full suite：633 tests；616 passed、17 skipped、3170 assertions
Frontend full suite：16 files／80 tests
Pint、lint、typecheck、production build：passed
```

## 5. Git 與封板

- v1.5 各工程／測試階段已有可追溯 commit；第 15 部分基準為 `a4bf025`。
- 第 16 部分文件與本次 Claude review follow-up 尚待使用者自行 commit，本輪不自動 commit
  或 push。
- 使用者 manual smoke 已通過，因此已具備準備 tag 的前提。
- 尚未建立 annotated tag；只有使用者明確授權後才可建立。
