# ERPV2 v1.5 第 10 部分交接

日期：2026-07-27

## 完成範圍

- 完成 `PLAN_v1.5.md` 第 10 部分「Login 與強制修改密碼 UI」。
- Login 已由 Email-only 改為「帳號名稱或 Email」，送出既有 `login` API 欄位，並依 `must_change_password` 導向強制頁或 Dashboard。
- 以正式三欄密碼表單取代 `/change-password` 暫存頁；該頁維持獨立 `passwordChangeOnly` gate，不掛一般 AppLayout／Sidebar。
- 強制頁已具備 visible labels、正確 autocomplete、per-field errors、loading／double-submit protection、Theme Toggle、登出與 Mobile safe area。
- 密碼修改成功會使用第 9 部分既有 `updatePassword()` 更新完整 Auth Context，再以 replace navigation 進入 Dashboard。
- API client 已集中辨識 HTTP 401 並通知 Auth Context 清除失效 Session，涵蓋一般 Session 過期與 admin reset 後下一個 stateful request。
- 因第 10 部分要求 Login title／subtitle 讀取 app config，先建立型別穩定的 `frontend/src/config/app.ts`；本階段只套用 Login。Sidebar、document title 與完整硬編碼稽核仍屬第 12 部分，未提前勾選。

## 正式契約

### Login

- state 與 payload 均使用 `login`，input type 為 `text`，允許 username 或 Email。
- Login input 使用 `autocomplete="username"`；密碼使用 `autocomplete="current-password"`。
- 登入成功回傳的完整 User 是導向依據：flag=true 進 `/change-password`，flag=false 進 `/dashboard`。
- 既有 API 通用錯誤與 429 訊息仍直接顯示；network／非標準錯誤使用安全 fallback。
- Login title 與 subtitle 取自 app config，不再由元件直接硬編碼。

### 強制修改密碼

- 表單只送出 `current_password`、`password`、`password_confirmation`。
- 目前密碼使用 `current-password` autocomplete；新密碼與確認欄使用 `new-password`。
- 前端先阻擋明確的 confirmation 不一致；後端 422 欄位錯誤回到對應欄位，輸入值不會因失敗清空。
- 只有 self-password API 成功並由 Auth Context 接受同一 User 的完整回應後才進 Dashboard；失敗時維持強制頁。
- 成功後後端回傳 flag=false，Auth Context 隨完整 User 更新，因此既有 Protected Route 不會形成 redirect loop。
- 送出期間所有欄位與兩個動作均 disabled；不會重複送出或在送出中登出。

### Session 失效

- Axios response interceptor 對所有 HTTP 401 發出 module-private Session invalidated event。
- Auth Provider 只在 logout 狀態為 idle 且仍有目前 User 時清除 Context；不覆蓋 logout pending／blocked 的安全畫面。
- Authenticated route 在 User 清除後由既有 Protected Route 導回 Login。
- 強制修改密碼請求本身收到 401 時亦明確 replace 導向 Login。
- admin reset 的舊 Session 不會被誤判成 `PASSWORD_CHANGE_REQUIRED`；使用者需以新預設密碼重新登入，再依登入回應 flag 進強制頁。

## 修改檔案

Frontend：

- `frontend/src/App.tsx`
- `frontend/src/api/client.ts`
- `frontend/src/auth/authSessionInvalidated.ts`
- `frontend/src/auth/authSessionInvalidated.test.ts`
- `frontend/src/auth/currentUserState.ts`
- `frontend/src/auth/currentUserState.test.ts`
- `frontend/src/config/app.ts`
- `frontend/src/hooks/useAuth.tsx`
- `frontend/src/pages/Login.tsx`
- `frontend/src/pages/PasswordChangeRequired.tsx`
- `frontend/src/pages/PasswordChangeRequiredPlaceholder.tsx`（移除）
- `frontend/src/pages/authFormState.ts`
- `frontend/src/pages/authFormState.test.ts`

文件：

- `PLAN_v1.5.md`
- `docs/v1.5-phase10-handoff.md`

## 自動驗證

```text
cd frontend && npm test
10 files／43 tests passed

cd frontend && npx tsc -b --noEmit
passed

cd frontend && npm run lint
0 errors；2 個既有 Fast Refresh warnings

cd frontend && npm run build
passed；production JS 513.31 kB（gzip 138.04 kB），只有既有 chunk size warning

git diff --check
passed
```

測試直接涵蓋：

- Login 依 `must_change_password` 選擇 Dashboard／強制頁。
- auth API 使用 `login` 而非舊 `email` payload。
- 422 欄位錯誤只映射允許的表單欄位，非 API error 使用安全 fallback。
- 只有 HTTP 401 觸發 Session invalidated 通知，且預設訂閱／unsubscribe 接線實際執行。
- 401 只清除 idle User，不覆蓋 logout pending／blocked。
- 第 9 部分既有 login/me flag、Protected Route、409、跨分頁與 logout 回歸測試持續通過。

## 未執行

- Backend tests：本部分沒有後端、API、migration、transaction 或權限程式變更；沿用第 8 部分完整 backend regression。
- MariaDB gated integration：本部分沒有 database 或 concurrency 變更。
- Browser manual smoke：留待第 15 部分由使用者執行完整新帳號、重設密碼、Mobile／dark mode 流程；本階段以 build 與純契約測試驗證。

## 風險與下一部分

- `frontend/src/config/app.ts` 已先建立以滿足 Login 契約，但 Sidebar 與 `document.title` 尚未套用；完整集中化必須在第 12 部分完成，現階段不可宣稱只改 config 就能全站同步。
- 本專案尚未引入 DOM／E2E 測試框架；第 10 部分的畫面 focus、Mobile safe area 與 password manager 行為仍需在 Browser Manual Smoke 實機確認。
- 下一部分依 `PLAN_v1.5.md` 第 11 部分完成「我的帳號」頁與 Header 入口，沿用本階段的 self API、Auth Context update 與表單錯誤 helper。
