# ERPV2 v1.6 Smoke Report

日期：2026-07-31

狀態：工程、自動回歸、對立審查與使用者整體 manual smoke 通過；使用者已
明確授權封板，`v1.6-smoke-passed` annotated tag、main 與 tag 已推送。

## 1. 範圍與基準

- 起始基準：`539599d`／`v1.5-smoke-passed`
- v1.6 runtime 基準：`35e1669 fix：關閉 Browser Smoke 複核問題`
- 使用者結果 commit：`3af5f11 docs：記錄 v1.6 使用者 Smoke 通過`
- 範圍：純前端 UX Copy、既有狀態條件式呈現、必要 Accessibility 關聯與測試
- Backend、schema、migration、API、route、roles、permissions、Business
  Logic、Workflow、package dependency：零 diff

## 2. 自動驗證

```text
Frontend：27 files／150 tests passed
lint：0 errors；2 個既有 Fast Refresh warnings
typecheck：passed
production build：1902 modules
JS：528.79 kB／gzip 141.42 kB
CSS：33.59 kB／gzip 7.32 kB
Backend：633 tests；616 passed、17 skipped、3170 assertions
git diff --check：passed
```

17 個 Backend skipped tests 為 v1.5 已存在的 environment-gated 或已取代測試。
v1.6 沒有 Backend diff。MariaDB 專用測試會操作明確允許的可拋棄 schema；
本版不涉及 schema、transaction、timezone 或競態，因此未重跑，沿用截至
v1.5 封板前已存在的專用證據，不宣稱本次通過。

## 3. 工程 Browser Smoke

- Browser：Google Chrome for Testing `151.0.7922.47`
- Driver：ChromeDriver `151.0.7922.47`
- Screen reader：Orca `46.1`
- Viewport：1440×1000、768×1024、390×844、375×667、320×568
- Theme：light、dark
- 角色：admin、manager、sales
- Chrome WebDriver：32 checks passed
- Orca：登入錯誤、第一個欄位錯誤、帳戶名稱 speech output 各 1 次
- Print：車輛建檔與成交結案 PDF 產生及逐頁視覺檢查通過

涵蓋 Dashboard、Account、User、Vehicle、MoneyEntry、Customer、Salary、
Audit、Cash Account、Modal／Drawer、Safe Area、focus return、角色遮蔽、
approved-only、成交月份、薪資鎖定、發薪與期末餘額。完整工程紀錄見
`docs/v1.6-phase-smoke-report.md`。

## 4. 使用者 Manual Smoke

使用者於 2026-07-30 回報：

```text
smoke 通過
```

PLAN 第 15 章依此整體結論勾選。使用者沒有另提供逐項結果、Desktop
browser/version、iPhone model／iOS／Safari、VoiceOver、admin／manager／sales
個別結果或受測 build commit；本報告不補寫未回報資訊。沒有回報失敗項目或
要求恢復文案。

## 5. 對立審查

- 第 14 章完整 runtime review：zero finding。
- 第 15 章安全版 manual checklist：Claude Opus 5／High 最終
  `ZERO FINDINGS`。
- 使用者結果與證據邊界 follow-up：`ZERO FINDINGS`。
- 第 16 章文件與交接 follow-up：`ZERO FINDINGS`。
- 第 17 章 Git／封板前稽核：`ZERO FINDINGS`。
- 第 18 章完成定義與最終全套回歸：`ZERO FINDINGS`。

有效 findings 均由 Codex 回到 runtime／測試查證後以最小修正處理；沒有為
消除評論而新增 Backend、schema、互動平台或範圍外功能。

## 6. 已知限制

- 使用者 manual smoke 只有整體通過結論，沒有逐裝置、逐角色或 VoiceOver
  明細。
- 照片排序「←」「→」缺 accessible name 是 v1.2 既有行為，v1.6 未改該
  控制；若要修正應另立 hotfix。
- lint 保留 2 個既有 Fast Refresh warnings，production build 保留既有
  >500 kB chunk warning。
- 本版未重跑 MariaDB 專用整合；原因與證據邊界見第 2 節。

## 7. 封板狀態

自動驗證、工程 Browser Smoke、對立審查、使用者整體 manual smoke 與文件
交接已完成。使用者於 2026-07-31 明確授權封板、建立
`v1.6-smoke-passed` annotated tag（message：`v1.6 Smoke passed`），以及
push main 與 tag；上述封板操作均已完成。
