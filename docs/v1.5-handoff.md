# ERPV2 v1.5 交接

日期：2026-07-29

## 完成狀態

`PLAN_v1.5.md` 第 0～16 部分已完成。v1.5 的工程、自動回歸、MariaDB username 專用整合、Firefox／Desktop Chrome 工程預驗證、使用者 Desktop Chrome／Mobile Safari Browser Manual Smoke、最終安全審查與文件同步均已完成。Manual smoke 基準為 `a4bf025`；smoke 後 runtime 修正基準為 `3f86871`。

`3f86871` 的登入、強制改密碼導流與我的帳號修正均有直接回歸測試。本輪封板審查重新查證
核心實作並重跑完整 Backend／Frontend 驗證，判定不需額外人工複驗。使用者已明確授權，
`v1.5-smoke-passed` annotated tag 已建立於本地，尚未推送至 origin。

## 正式能力

```text
Admin 建立員工／重設密碼
→ must_change_password=true
→ 員工以 Email 與預設密碼登入
→ 後端與前端強制修改密碼
→ 可在我的帳號設定 username
→ 後續可用 username 或 Email 登入
```

- username 為 nullable、3～30 字元、正規化小寫、case-insensitive unique。
- username／Email 是同一 User 的 alias，共用 canonical Rate Limiter。
- self profile 只能改 name／username；self password 必須驗目前密碼、confirmation 且不能沿用舊密碼。
- Email、role、is_admin、is_active 與員工欄位不能由 self API 修改。
- 密碼不進 Resource、Audit 或一般 log。
- 系統識別集中在 `frontend/src/config/app.ts`，仍是 source-controlled config。

## 驗證摘要

```text
Backend：633 tests；616 passed、17 skipped、3170 assertions
Frontend：16 files／80 tests
MariaDB username integration：2 tests／36 assertions
Frontend lint／typecheck／build：passed
Firefox：38 checks passed
Chrome：15 checks passed
App config：2 checks passed
使用者 manual smoke：33 項 passed（基準 `a4bf025`）
第 16 部分安全審查：整體通過；3 個有效 low findings 已修正並補測
```

完整證據見 `docs/v1.5-smoke-report.md`；browser driver 細節見 `docs/v1.5-phase15-smoke-report.md`。

## Claude Review Follow-up

- 停用帳號正確密碼不再清除登入 limiter，並會累積 IP-wide failure。
- `409 PASSWORD_CHANGE_REQUIRED` 已與 401 一樣受 request generation 保護，舊 Session
  回應不能污染新登入 Context。
- 個人資料已提交但 Context 已切換時，前端改顯示已更新並要求重新登入確認，不再誤報失敗。
- 登入 timing oracle 疑慮經 Laravel Timebox 實測否定。
- `PATCH /api/me/password` 額外 per-user throttle 屬規格外 hardening，未直接加入 v1.5。
  若要採用，需先決定門檻、key、正確密碼請求是否計數及 429 UX，再另立 hotfix PLAN。

## 部署與相容性

- 部署需執行一般 `php artisan migrate`；migration 不回填 username，也不把既有 User 設為待改密碼。
- 既有帳號升級後保持 `username=null`、`must_change_password=false`，可繼續用 Email 登入。
- 前端登入 payload 已由 `email` 改為 `login`；舊 client 若仍傳 `email` 會取得 422，需與 v1.5 前端一併部署。
- 調整公司／系統名稱只修改 `frontend/src/config/app.ts` 並重新 build；沒有線上設定頁。
- 認證只支援 Sanctum SPA cookie／Session，不支援 Bearer token。

## 禁止擴張

v1.5 不包含自助註冊、忘記密碼 Email、Email 驗證、MFA／SSO、裝置／Session 管理、Database settings、線上系統設定、多公司品牌或 Theme server-side 個人設定。後續需求必須另立企劃與 PLAN。

## 下一步

1. v1.5 已封板，不再回開帳號自助管理或系統識別範圍；後續缺陷以獨立 hotfix PLAN 處理。
2. 本地 `v1.5-smoke-passed` annotated tag 尚未推送至 origin；需由使用者另行執行或授權 push。
3. 下一階段依產品決策另立企劃、PLAN 與新聊天室，不將新功能繼續塞入 v1.5。

建議 commit message：

```text
docs：對齊 v1.5 runtime 與 smoke 基準
```
