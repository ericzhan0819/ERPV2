# ERPV2 v1.5 第 3 部分交接

日期：2026-07-23

## 完成範圍

- 完成 `PLAN_v1.5.md` 第 3 部分 Login Request 與 Email／username 雙識別登入。
- 更新 Auth API 文件、`UserResource` 輸出與登入相關自動測試。
- 未實作第 4 部分 canonical account Rate Limiter；目前 username 與 Email alias 仍各自使用既有 account limiter key。
- 未修改前端登入 API／頁面；前端切換 `login` payload 與文案仍屬後續 PLAN 範圍。

## 實作內容

- `LoginRequest` 正式由 `email` 改為 `login`，契約為 required、string、最大 255 字元；密碼維持 required string。
- `AuthController` 只傳遞 validated `login` 與 password，維持薄 Controller。
- `AuthService` 先移除識別字前後空白，再依是否包含 `@` 分流：
  - Email：轉小寫後查找正式 Email，確保 SQLite 與 MySQL／MariaDB 都維持大小寫不敏感。
  - username：沿用 `User::normalizeUsername()`，以既有小寫儲存契約登入。
- 不存在帳號與密碼錯誤都只回 `帳號或密碼錯誤`，不洩漏識別字是否存在。
- 停用帳號以 Email 或 username 登入都會登出並回既有停用訊息。
- 成功登入保留 Session regenerate 與 Authentication Audit；稽核寫入失敗仍會使 Session 失效。
- `UserResource` 新增 `username` 與 `must_change_password`，登入與 `/api/me` 共用相同契約。
- 既有 identifier + IP、account、IP-wide 三層 Rate Limiter、429 與 `Retry-After` 均保留；alias 共用 canonical account key 留待第 4 部分。

## 自動測試

聚焦登入、限流與稽核回歸：

```text
php artisan test --filter='DualIdentifierLoginTest|LoginThrottleTest|AuditLogTest'
21 tests passed，200 assertions
```

完整後端回歸：

```text
php artisan test
567 tests：550 passed，17 environment-gated skipped，2660 assertions
```

格式：

```text
vendor/bin/pint --test ...
passed
```

前端既有品質檢查：

```text
npm run test -- --run
14 tests passed

npm run lint
passed；保留 2 個既有 Fast Refresh warnings

npm run build
TypeScript 與 production build passed；保留既有大於 500 kB chunk warning
```

## 未執行

- 未執行 browser manual smoke：前端登入 API 仍送出 `email`，依 PLAN 留待後續前端階段切換成 `login`。
- 未執行 MySQL／MariaDB gated integration：本部分沒有 migration 或並發寫入；資料庫差異所需的 Email 大小寫行為已以 SQLite feature test 保護，既有 MariaDB username unique 測試仍由 environment gate 管理。

## 後續邊界

- 第 4 部分必須解析同一 User 的 Email／username alias，讓兩者共用 canonical account limiter，不能沿用本階段各識別字分開的 account key。
- 第 4 部分改 limiter key 時需保留 IP-wide、identifier + IP、成功登入清除範圍、阻擋後不再驗證密碼、429 與 `Retry-After`。
- 前端正式切換前，整合環境的登入頁無法配合新 request key；不得以後端同時接受舊 `email` 欄位規避已確認的新契約。
