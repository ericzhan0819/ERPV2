# ERPV2 v1.5 第 6／7 部分交接

日期：2026-07-25

## 完成範圍

- 完成 `PLAN_v1.5.md` 第 6 部分「Password Change Required Middleware」。
- 依 PLAN 的強制交付相依，同輪完成第 7 部分「Self Account Backend API」，避免啟用 gate 後沒有可清除 `must_change_password` 的端點。
- 未提前實作第 8 部分 UserResource／前端 API types、第 9 部分 Auth Context／Route Gate 或第 10～11 部分前端頁面。

## 正式契約

### Password Change Required Gate

Authenticated 營運路由依序執行：

```text
auth:sanctum
→ active
→ password.changed
→ role（若有）
→ route model binding
→ policy（若有）
```

`must_change_password=true` 時固定回：

```json
{
  "message": "請先修改密碼",
  "code": "PASSWORD_CHANGE_REQUIRED"
}
```

HTTP status 為 `409 Conflict`。Gate 位於角色檢查與 route model binding 前，避免待改密碼帳號利用 `403`／`404` 差異枚舉營運資源。停用帳號仍優先回既有 `403`，未登入仍由 Sanctum 回 `401`。

唯一 allowlist：

```text
GET   /api/me
POST  /api/logout
PATCH /api/me/profile
PATCH /api/me/password
```

`POST /api/logout` 維持既有公開冪等路由；Login、Sanctum CSRF 與 `/api/public/vehicles*` 不受 gate 影響。測試會掃描所有 `auth:sanctum` 路由，除三個 self account 例外外都必須帶 gate，避免未來新增 authenticated route 時意外漏保護。

### Self Profile

`PATCH /api/me/profile` 只操作 `$request->user()`，不接受 target user id。

- 必須完整提交 `name` 與 `username`。
- `username=null` 或空字串代表明確清除；非 null 值沿用共用正規化、格式與 unique 規則。
- Email、role、admin／active 狀態、員工欄位、`must_change_password` 與 password 只要出現在 payload 就以 `missing` rule 回 `422`。
- Database unique race 仍轉為 `username` 欄位 `422`，不回未處理的 `500`。
- Name 與 username 使用單一 `save()`，並與 User Audit 位於同一個 transaction；Audit 失敗時完整 rollback。
- 成功回傳更新後 `UserResource`，不含 password／remember token。

### Self Password

`PATCH /api/me/password` 只操作 `$request->user()`：

- `current_password` 必須通過 Laravel `current_password:web` rule。
- 新密碼 required、string、至少 8 字元且 confirmed。
- Password 與 `must_change_password=false` 使用單一 `save()`，並與 User Audit 位於同一個 transaction。
- 成功後 regenerate 目前 Session ID；下一個 stateful request 仍保持登入，不會被 Sanctum `AuthenticateSession` 踢出。
- 成功回傳更新後 `UserResource`。
- Audit 只記錄 `must_change_password` 狀態變更，不含明文、hash、目前密碼或 confirmation。

## 修改檔案

Backend：

- `backend/app/Http/Controllers/CurrentUserController.php`
- `backend/app/Http/Middleware/EnsurePasswordHasBeenChanged.php`
- `backend/app/Http/Requests/UpdateCurrentUserPasswordRequest.php`
- `backend/app/Services/UserService.php`
- `backend/bootstrap/app.php`
- `backend/routes/api.php`
- `backend/tests/Feature/CurrentUserAccountTest.php`
- `backend/API.md`

文件：

- `PLAN_v1.5.md`
- `docs/v1.5-phase6-handoff.md`

## 自動驗證

專項與既有高風險回歸：

```text
php artisan test --filter='CurrentUserAccountTest|UsernameValidationTest|UserTest|AuditLogTest|LoginThrottleTest|RoleAccessTest'
125 tests passed，807 assertions
```

完整後端回歸：

```text
php artisan test --log-junit /tmp/erpv2-v15-phase6-junit.xml
613 tests：596 passed，17 environment-gated skipped，3058 assertions
0 errors，0 failures
```

格式：

```text
vendor/bin/pint --test <本輪 PHP 修改檔案>
passed
```

## 未執行

- MariaDB gated integration：本輪沒有 migration；完整回歸中的 17 項既有 MariaDB／環境 gated 測試依條件跳過。Username DB unique race 的 MySQL／MariaDB 基礎已由第 2 部分既有 integration test 保護，本輪沿用同一個 constraint 判斷 helper。
- Frontend test／lint／typecheck／build：本輪依第 6＋7 部分邊界只修改後端與文件，沒有前端程式變更。
- Browser manual smoke：強制改密碼頁、Auth Context 導流與「我的帳號」頁尚屬後續第 9～11 部分，現階段沒有完整可驗收 UI。

## 部署與後續邊界

本階段是後端完整安全切片，但不是可單獨部署的 v1.5 使用者體驗。現有前端尚未處理 `PASSWORD_CHANGE_REQUIRED`，也沒有自助密碼表單；待改密碼帳號在瀏覽器內會被營運 API 阻擋，但無法透過 UI 完成解鎖。

因此第 5～7 部分仍不得單獨部署到正式環境，尤其是只有單一 admin 的環境。下一部分從 `PLAN_v1.5.md` 第 8 部分開始，接續 API types，之後完成 Auth Context、集中 409 導流與強制改密碼／我的帳號頁。
