# ERPV2 v1.5 第 0～1 部分交接

日期：2026-07-23

## 完成範圍

- 完成 `PLAN_v1.5.md` 第 0 部分前置盤點與基準驗證。
- 完成第 1 部分 User Schema、Model、Factory、Seeder 與自動測試。
- 未提前實作第 2 部分 username 完整驗證、雙登入、強制改密碼流程或前端 UI。

## 實作內容

- 新增 `users.username` nullable string 與 `users_username_unique` unique index。
- 新增 `users.must_change_password` boolean，database default 為 `false`。
- Migration 不回填 username、不推測既有帳號狀態，並可在 MySQL 部分 DDL 後安全重跑。
- Down migration 先移除 unique index，再移除兩個欄位。
- `User` 加入 fillable、boolean cast 與 `username` Attribute mutator；所有 Eloquent 寫入都強制經過 `User::normalizeUsername()`，不再依賴呼叫端自行記得正規化。
- Username 正規化使用 `Str::trim()` 處理 ASCII、全形與 non-breaking whitespace，再轉為小寫；純空白結果為 `null`。
- Factory 預設維持既有測試語意，新增 `mustChangePassword()` 與 `withUsername()` states。
- `AdminUserSeeder` 明確維持 `must_change_password=false`，重跑時不覆蓋已設定的 username。

## 第 0 部分盤點結果

- 工作開始時 `main` 與 `origin/main` 同步，工作樹乾淨；HEAD 為 `87d415f`。
- `v1.4-smoke-passed` 為 annotated tag，解析後指向 `d4ea978`。
- Auth 目前仍只接受 `email + password`；Rate Limiter 目前使用 email/IP、account 與 IP 三層 key。
- `/api/me` 與一般營運 API 目前位於 `auth:sanctum + active` 群組。
- 前端 Auth Context、ProtectedRoute、AppLayout、User List、Theme Provider 與兩份 User types 均已盤點。
- 正式系統名稱硬編碼位於 `frontend/index.html`、`frontend/src/pages/Login.tsx`、`frontend/src/layouts/AppLayout.tsx`；留待第 12 部分集中化。
- 已確認 v1.5 不建立線上 Settings、MFA、Email reset、SSO、官網功能或其他範圍外功能。

## 驗證

實作前基準：

```text
backend: 519 tests，504 passed，15 environment-gated skipped，2516 assertions
frontend: 14 tests passed；lint 通過（2 個既有 Fast Refresh warnings）；typecheck、production build 通過
```

第 1 部分：

```text
php artisan test --filter=UserAccountSchemaTest
15 tests passed，28 assertions

vendor/bin/pint --test ...
passed
```

MariaDB 10.11.18 gated integration test：

```text
APP_ENV=testing \
DB_CONNECTION=mysql \
DB_DATABASE=erpv2_v15_user_account_test \
RUN_MYSQL_CONCURRENCY_TESTS=1 \
MYSQL_CONCURRENCY_TEST_CONNECTION=mysql \
MYSQL_CONCURRENCY_TEST_DATABASE=erpv2_v15_user_account_test \
php artisan test --filter=UserAccountMysqlIntegrationTest

1 test passed，17 assertions
Eric／eric 由 utf8mb4_unicode_ci unique index 視為重複
migrate:rollback --step=1 會移除索引後再移除欄位
重新 migrate 通過
驗證後已刪除可拋棄 schema
```

開發資料庫同步：

```text
php artisan migrate --force
v1.5 migration 通過
既有 3 個帳號均保持 username=null、must_change_password=false
未執行 AdminUserSeeder，避免重設既有 admin 密碼
```

Adversarial review 修正後完整回歸：

```text
backend: 535 tests，519 passed，16 environment-gated skipped，2544 assertions
frontend: 14 tests passed；lint 通過（2 個既有 Fast Refresh warnings）；typecheck、production build 通過
```

## Adversarial review 處理

- Finding 1 有效：已以 Attribute mutator 建立不可繞過的 Eloquent normalization boundary，並補 SQLite case-variant 與 gated MariaDB collation 測試。
- Finding 2 有效：開發資料庫已安全 migrate；未執行會重設密碼的 Seeder。
- Finding 3 有效：改用 `Str::trim()`，並補 U+3000、U+00A0、純空白與混合大小寫測試。
- Finding 4 為後續有效提醒：目前 Controller／Service 白名單使欄位不可被利用；第 5 部分必須依 PLAN 對 Store／Update Request 加入 `missing` 與 payload 回歸測試，本階段不提前實作。
- Finding 5 有效：SQLite 直接 migration 測試明確限制為 SQLite；真正 migrator up/down 順序改由 gated MariaDB integration test 自動保護。

## 後續邊界

- 第 2 部分需沿用 mutator 與 `User::normalizeUsername()`，並對正規化後的值補齊 nullable、長度、字元白名單、繁體中文訊息、application unique validation 與競態 422。
- 第 3～4 部分才調整 Login Request、AuthService 與 alias-safe Rate Limiter。
- 第 5 部分才讓 admin 建立／重設密碼明確設 `must_change_password=true`；目前 database、Factory 與既有帳號預設皆保持 `false`。
- 尚未更新 `UserResource` 或前端 User types，這些屬第 8 部分。
