# ERPV2 v1.6 實作進度

日期：2026-07-29
起始 commit：`539599d`
起始 tag：`v1.5-smoke-passed`

本文件累積記錄 `PLAN_v1.6.md` 各主章節的實作、驗證、commit 與 Claude Opus 5 對立審查結果。只有完成直接驗證且通過零 finding 閘門的章節，才能進入下一章。

## 固定審查方式

- Reviewer：本機 Claude Code；第 0～1 章使用 `--model opus --effort xhigh`，自第 2 章起依使用者授權改用 `--model opus --effort high`
- 權限：唯讀／plan，不允許修改工作樹
- Finding 門檻：必須指出具體位置、可觀察失敗模式及最小修正方向
- Codex 對每項 finding 回到程式碼與測試查證；有效項目修正並補測，無效或越界項目以證據交回複核
- Gate：Claude 明確回覆沒有新的可重現、可觀察、v1.6 範圍內 finding

## 第 0 章：前置盤點與範圍鎖定

狀態：完成；Claude 零 finding gate 已通過。

- 基準確認：`HEAD` 與 `v1.5-smoke-passed^{}` 均為 `539599d59aee23cd0ed97161193642340b8d9f34`。
- 使用者交接變更：`企劃書_v1.6.md`、`PLAN_v1.6.md` 與 v1.5 push 狀態／v1.6 邊界文件同步，均視為有意變更。
- 正式邊界：不修改 backend、migration、API contract、roles、permissions、business logic、Dashboard 統計、URL Filter 或 runtime dependency。
- 完成 pages、components、輔助文字、頁面副標、Card description、`aria-describedby`、`title=` 與文字型測試 selector 搜尋。
- 建立 `docs/v1.6-copy-inventory.md`，記錄逐頁 A／B／C／D／E 初步決策與共用 accessibility 契約。
- 本章沒有 runtime 變更，不執行 Frontend／Backend suite；驗證採文件 diff、Git 基準與靜態搜尋。

### 驗證

```text
git rev-parse HEAD
git rev-parse v1.5-smoke-passed^{}
find frontend/src/pages -type f
find frontend/src/components -type f
rg text-fg-muted|text-fg-subtle|description|help|hint|warning|placeholder
rg aria-describedby|title=
rg tests 內文字 query／assertion
git diff --check
```

結果：

- `HEAD` 與 tag 解引用一致，均為 `539599d59aee23cd0ed97161193642340b8d9f34`。
- 盤點 22 個正式 page TSX、10 個 `components/` 正式 component TSX，以及 1 個位於 `pages/salary/shared.tsx` 的薪資共用狀態元件。
- `git diff --check`：通過。
- Backend、migration、routes、前端 API／types 與 package manifests：無 diff。

### Commit 與 Claude review

- 主章節 commit：`b6221e4 docs：建立 v1.6 文案精簡規格與盤點基準`
- Claude 第一輪：整體通過，提出 1 個 Low finding。
- Finding 查證：有效。`pages/salary/shared.tsx` 只匯出 `SalaryStatusBadge`，不屬正式路由頁；原紀錄將 23 個非測試 TSX 全部計為頁面。
- 修正：正式頁面數更正為 22，明確列入 `SalaryStatusBadge` D 類狀態契約，並補記 AppLayout／Header／Sidebar 邊界。
- Follow-up commit：`d248867 docs：校正 v1.6 頁面與共用狀態盤點`
- Claude 第二輪：確認上一輪 finding 完整關閉，沒有新的可證明 findings。
- 零 finding gate：通過。

## 第 1 章：更新 UI Copy Design Contract

狀態：完成；Claude 零 finding gate 已通過。

- 在 `UI.md` 新增正式「UI Copy 與輔助文字」章節。
- 定義頁面／section／Card／form help text 的保留條件與 A／B／C／D／E 五分類。
- 分離 warning、error、success、empty state 與 disabled reason 的責任。
- 明定 tooltip、placeholder、`aria-describedby`／`aria-labelledby`、`sr-only` 與 Mobile 資訊減法邊界。
- 明定文案風格、共用元件抽取條件及不新增 Database-backed copy、i18n、analytics、onboarding 或 dependency。
- 本章只修改 Design Contract、PLAN 與進度紀錄，沒有 runtime 變更。

### 驗證

```text
git diff --check
rg "## 15. UI Copy" UI.md
rg "A：直接刪除|B：縮短保留|C：條件式顯示|D：必須保留|E：無障礙專用" UI.md
git diff -- backend frontend/src frontend/package.json frontend/package-lock.json
```

結果：

- `git diff --check`：通過。
- `UI.md` 第 15 章與五分類可由靜態搜尋定位。
- Backend、frontend runtime 與 package manifests：無 diff。

### Commit 與 Claude review

- 主章節 commit：`05ac737 docs：建立 v1.6 UI Copy 設計契約`
- Claude findings：沒有 findings。
- Follow-up commits：無
- 零 finding gate：通過。
- 後續風險：Design Contract 不代表 runtime 已符合；第 2～10 章仍須逐頁驗證 D 類口徑、條件式提示與 accessibility。

## 第 2 章：全站頁面標題與副標清理

狀態：完成，零 finding gate 已通過。

- 逐一檢查 22 個正式頁面、App routes、Protected Route 特殊狀態與 AppLayout 導覽。
- 移除 Dashboard、Login、Cash Account、Customer Create／List、Money Entry List、Salary Period List、User List、Commission Attribution、Vehicle Create／List 的重述用途副標。
- Login 同步移除不再使用的 `loginSubtitle` 前端 config 欄位與對應測試期待；登入識別 label、錯誤與流程不變。
- 保留 Password Required、Account、Audit、Money Entry Create、Commission Plan、Salary Profile 等非直覺權限、風險或資料口徑文字，留待對應模組章節縮短。
- 保留 Customer Detail 類型、Vehicle Detail 品牌／車型、Salary Detail 適用方案等正式資料，不視為教學副標。
- 車輛建檔與成交列印主標改用相同文字與樣式的 `h1`，不改 print data 或 CSS。
- 未新增 404 route；現有權限與 Session 特殊狀態文字及 route gate 均不變。

### 驗證

```text
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
rg "<h1" frontend/src/pages
git diff -- backend frontend/src/api frontend/src/types frontend/package.json frontend/package-lock.json
```

結果：

- `npm test`：16 files／80 tests passed。
- `npm run lint`：0 errors；2 個既有 Fast Refresh warnings。
- `npm run typecheck`：通過。
- `npm run build`：通過；1900 modules，保留既有 >500 kB chunk warning。
- `git diff --check`：通過。
- 被移除的副標與 `loginSubtitle` 已無殘留；22 個正式頁面的正常內容分支均有 `h1`，User List 的兩個 `h1` 位於互斥權限分支。
- Backend、前端 API／types、package manifests：無 diff。

### Commit 與 Claude review

- 主章節 commit：`e04dbbd ui：精簡全站頁面標題與副標`
- Claude findings：沒有新的可重現、使用者可觀察且在 v1.6 範圍內的 finding。
- Follow-up commits：無
- 零 finding gate：通過。
- 後續追蹤：Account 與 Audit 的 A 類文字分別留在第 4、8 章處理；既有專案沒有 404 route，本章只完成現況檢查，不新增禁用範圍外頁面。

## 第 3 章：Dashboard 文案精簡

狀態：完成，第一輪 Claude findings 已修正，第二輪零 finding gate 已通過。

- 工作概況保留區塊標題，移除操作教學與四張 KPI 的重複 description；Card 的 label、value、unit、icon、Link 與 query string 不變。
- KPI Card 由 `min-h-40` 調整為一致的 `min-h-36`，刪除 description 後仍維持 Grid 對齊。
- 經營概況只對 admin／manager 集中說明完整月份、收入／支出的收支日期歸屬、毛利／成交的成交日期歸屬、approved-only 與正式帳面餘額；sales 不顯示不適用的財務說明。
- 在庫數保留最短的「含保留中」定義，其他 Card 重複 description 維持移除。
- 趨勢期間縮為「近 30 天（含今天）」；成交量移除重複每日統計說明，毛利保留成交日與 approved-only，現金保留每日期末帳面餘額。
- `DashboardTrendChart` 的 description 改為選填；圖表 title、視覺單位、`aria-labelledby`、SVG、points、empty state 與 `formatValue` 不變。
- 新增 Dashboard presentation tests，覆蓋 KPI Link、三角色可見性、區塊級口徑唯一性與圖表 accessible name。

### 驗證

```text
npm test -- --run src/pages/Dashboard.test.tsx src/utils/dashboardPresentation.test.ts
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
git diff -- backend frontend/src/api frontend/src/types frontend/package.json frontend/package-lock.json
```

結果：

- Dashboard 直接測試：2 files／11 tests passed。
- `npm test`：17 files／86 tests passed。
- `npm run lint`：0 errors；2 個既有 Fast Refresh warnings。
- `npm run typecheck`：通過。
- `npm run build`：通過；1900 modules，保留既有 >500 kB chunk warning。
- `git diff --check`：通過。
- Backend、前端 API／types、package manifests：無 diff。
- Browser 實機目視尚未執行；依 PLAN 第 13 章統一覆蓋 light／dark 與各 viewport。

### Commit 與 Claude review

- 主章節 commit：`5ef726d ui：精簡 Dashboard 文案與資訊層級`
- Claude findings：
  - F1（中）：區塊短句遺失毛利／成交依成交月份歸屬，且未區分收入／支出的收支日期口徑。有效；補回兩種日期歸屬並加測。
  - F2（低）：sales 看不到財務 KPI，卻仍顯示財務口徑。有效；改由既有 `canViewFinance` 條件式顯示並加測。
  - F3（低）：在庫數是否包含保留中並不直覺。有效；只在在庫卡保留「含保留中」。
- Follow-up commits：`b69d868 fix：補足 Dashboard 統計口徑`
- 第二輪 Claude review：三項 findings 均已正確修復，沒有新的可重現、使用者可觀察且在 v1.6 範圍內的 finding。
- 零 finding gate：通過。
- 後續目視：第 13／15 章確認現金 KPI 與截至今日趨勢的辨識度，以及在庫卡多一行短定義後的實際 Grid 列高。

## 第 4 章：Auth、強制改密碼與我的帳號

狀態：完成，主 commit 與閉環文件修正均通過零 finding gate。

- Login 沿用第 2 章已精簡的頁頭；帳號名稱／Email visible label、空白 placeholder、通用錯誤、登入 payload 與 Auth Context 不變，未新增其他 Auth 功能。
- 強制改密碼說明縮為「此帳號使用預設密碼。修改後才能進入營運功能。」；新密碼欄旁保留至少 8 字元與不可沿用目前密碼。
- 我的帳號移除頁面及兩張 Card 的重述副標，Email 與角色 read-only 呈現不變。
- username help 依 null／已有 username 顯示 Email fallback 或清除後果，格式規則縮為 `3～30 字元，可用英數、._-；儲存時轉小寫`。
- username 與新密碼欄的 help／error ID 共同維護 `aria-describedby`，沒有 dangling ID。
- 我的帳號密碼規則移到新密碼欄旁；self profile／password payload、endpoint、Session invalidation 與 stale response handling 不變。
- 新增 Password Change Required 專屬 presentation／流程測試，並擴充 Login、Account 的條件文案、per-field error、成功與 stale response 回歸。

### 驗證

```text
npm test -- --run src/pages/Login.test.tsx src/pages/PasswordChangeRequired.test.tsx src/pages/account/Account.test.tsx
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
git diff -- backend frontend/src/api frontend/src/types frontend/package.json frontend/package-lock.json
```

結果：

- Auth／Account 直接測試：3 files／20 tests passed。
- `npm test`：18 files／96 tests passed。
- `npm run lint`：0 errors；2 個既有 Fast Refresh warnings。
- `npm run typecheck`：通過。
- `npm run build`：通過；1900 modules，保留既有 >500 kB chunk warning。
- `git diff --check`：通過。
- Backend、前端 API／types、package manifests：無 diff。
- Browser 實機目視尚未執行；依 PLAN 第 13 章統一覆蓋 light／dark 與各 viewport。

### Commit 與 Claude review

- 主章節 commit：`dadbaad ui：精簡帳號與密碼提示文案`
- Claude findings：沒有新的可重現、使用者可觀察且在 v1.6 範圍內的 finding。
- 閉環紀錄 review：1 項 Low 文件 finding；第 13／15 章沒有 screen reader checklist，無法承接原先延後的實際讀屏順序。
- 處理：有效；在 PLAN 10.3 新增 Account／強制改密碼 help 先於 error 的實際讀屏檢查。
- Follow-up commits：
  - `05ff99b docs：補上帳號表單讀屏驗證追蹤`
  - `28483c0 fix：校正 v1.6 審查章節狀態`
- 文件修正複核：沒有新的可重現、可觀察且在 v1.6 範圍內的 finding；第 0～4 章狀態與淨 diff 均一致。
- 零 finding gate：通過。
- 後續驗證：第 13／15 章確認兩行欄位 help 的窄螢幕換行；第 10.3／10.4 章確認 help／error 的讀屏順序與自動化關聯。

## 第 5 章：員工／帳號管理與資金帳戶

狀態：完成，主 commit 與閉環文件修正均通過零 finding gate。

- `/users` 維持 `ProtectedRoute allowedRoles={['admin']}` 與後端權限；元件內非 admin 的「僅限管理員操作」只保留為防禦性 fallback。
- 員工編輯列移除角色、啟停用與密碼控制位置教學；既有操作、payload 與 API 不變。
- 自己帳號的列將角色、停用、刪除與密碼限制整合為一則可見短句；相關 disabled controls 以同一 `aria-describedby` 引用，不再以 hover `title` 作為唯一原因。
- 建立員工的 visible labels、required marker、預設密碼成功結果、`must_change_password` Badge，以及重設後 Session 失效與再次強制改密碼結果均保留。
- 資金帳戶編輯列移除「使用列表按鈕」教學；目前餘額、狀態、啟用／停用操作、錯誤及刪除不可逆確認不變。
- 新增 UserList 與 CashAccountList presentation tests，覆蓋 self 限制、重設結果、正式餘額、角色寫入邊界、狀態操作與刪除確認。

### 驗證

```text
npm test -- --run src/pages/users/UserList.test.tsx src/pages/cash-accounts/CashAccountList.test.tsx
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
git diff -- backend frontend/src/api frontend/src/types frontend/package.json frontend/package-lock.json
```

結果：

- User／Cash Account 直接測試：2 files／6 tests passed。
- `npm test`：20 files／102 tests passed。
- `npm run lint`：0 errors；2 個既有 Fast Refresh warnings。
- `npm run typecheck`：通過。
- `npm run build`：通過；1900 modules，保留既有 >500 kB chunk warning。
- `git diff --check`：通過。
- Backend、前端 API／types、package manifests：無 diff。
- Browser 實機目視尚未執行；依 PLAN 第 13 章統一覆蓋表格橫向捲動、light／dark 與各 viewport。

### Commit 與 Claude review

- 主章節 commit：`95a11b7 ui：精簡員工與資金帳戶操作文案`
- Claude findings：沒有新的可重現、使用者可觀察且在 v1.6 範圍內的 finding。
- 閉環紀錄 review：
  - F1（Low）：第 10 章沒有 disabled self controls 實際讀屏的承接項。有效；在 PLAN 10.3 新增可見短句與 screen reader 取得檢查。
  - F2（Low）：第 13 章沒有寬表格橫向捲動與 self 短句換行的承接項。有效；在 PLAN 13.3 新增 320～768px 檢查。
- Follow-up commits：`4d796d2 docs：補上員工管理跨裝置驗證追蹤`
- 文件修正複核：沒有新的可重現、可觀察且在 v1.6 範圍內的 finding；新增的第 10.3／13.3 checklist 均有可執行承接點。
- 零 finding gate：通過。
- 後續驗證：第 10 章確認 disabled controls 對共用限制說明的實際讀屏行為；第 13 章確認 self 短句在窄螢幕表格中的換行與橫向捲動。

## 第 6 章：薪資模組文案分層

狀態：完成，零 finding gate 已通過。

- 薪資月份列表已於第 2 章移除用途副標；empty state 補成「選擇月份並建立草稿」的下一步，月份狀態、金額與建立限制不變。
- draft 當月未結束 warning 縮為原因＋下月確認，仍只在月份尚未結束時顯示，確認按鈕 disabled 條件不變。
- confirmed 新增單一區塊鎖定結果；paid 發薪結果補足薪資快照與車輛歸屬已鎖定、本月份唯讀。確認與發薪的不可逆 `window.confirm` 不變。
- 阻擋異常與「獎金設定提示（不阻擋確認）」維持不同語意與樣式；修正路徑、公司營運保留、公司剩餘分配額及手動加扣項「說明」不變。
- 獎金方案移除全頁常駐規則，只在 `is_used` Card 顯示「規則已鎖定；變更請建立新版本。」；未使用方案不顯示。
- 員工薪資設定的 snapshot 邊界縮為「只影響未確認月份；歷史薪資快照不變。」；欄位、狀態、empty state 與 API 不變。
- 待補獎金歸屬只在有待補車輛時說明缺少收／賣車人與補齊後重算草稿；不推測歷史值，成功後仍以該 row 消失呈現結果。
- 新增 Salary presentation tests，覆蓋 open／closed month、confirmed／paid、blocking／non-blocking、used／unused plan、empty state 與 attribution 成功結果。

### 驗證

```text
npm test -- --run src/pages/salary/SalaryPresentation.test.tsx
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
git diff -- backend frontend/src/api frontend/src/types frontend/package.json frontend/package-lock.json
```

結果：

- Salary 直接測試：1 file／9 tests passed。
- `npm test`：21 files／111 tests passed。
- `npm run lint`：0 errors；2 個既有 Fast Refresh warnings。
- `npm run typecheck`：通過。
- `npm run build`：通過；1900 modules，保留既有 >500 kB chunk warning。
- `git diff --check`：通過。
- Backend、前端 API／types、package manifests：無 diff。
- Browser 實機目視尚未執行；依 PLAN 第 13 章覆蓋狀態提示、Modal、Mobile Card／Table 與 light／dark。

### Commit 與 Claude review

- 主章節 commit：`209ee91 ui：分層精簡薪資模組文案`
- Claude findings：沒有新的可重現、使用者可觀察且在 v1.6 範圍內的 finding。
- Follow-up commits：無
- 零 finding gate：通過。
- 後續驗證：第 13 章確認 confirmed 提示的 light／dark 對比，以及 paid／待補歸屬長句在 375px 的換行與 Card 高度。

## 第 7 章：車輛模組

狀態：完成，主 commit、測試 follow-up 與閉環文件均通過零 finding gate。

- 車輛列表不再刪字：保留 active filter chips、至少一個狀態限制、頁碼超界／有篩選無結果／無在庫資料三種空狀態，以及無封面時的「尚無照片」。
- 新增車輛的 checkbox label 移除重複「是否」，同步購車付款仍明確表示建立時一併記錄；付款金額與帳戶缺失錯誤、payload 與固定 idempotency key 不變。
- 上架、收訂金、尾款、整備支出與成交 Modal 已逐一盤點；送出狀態、API error、成功後刷新、說明／備註欄位及角色條件不變。
- 收購價說明縮為「影響毛利與薪資獎金＋薪資月份鎖定後不可修改」；成交說明縮為「成交日期決定薪資月份＋鎖定月份阻擋＋收款日期不影響成交月份」。
- 車輛照片現況沒有按鈕用途重述，保留上傳錯誤與同 idempotency key 重試、封面、左右排序、刪除不可逆確認及所有 action error。
- 新增 Vehicle presentation tests，覆蓋 Filter chips、空資料／無結果／頁碼超界、無照片、sales 底價遮蔽、同步付款錯誤、成交高風險提示、admin／非 admin 整備支出結果、照片重試／排序／封面／刪除及收購價鎖定。

### 驗證

```text
npm test -- --run src/pages/vehicles/VehiclePresentation.test.tsx
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
git diff -- backend frontend/src/api frontend/src/types frontend/package.json frontend/package-lock.json
```

結果：

- Vehicle 直接測試：1 file／8 tests passed。
- `npm test`：22 files／119 tests passed。
- `npm run lint`：0 errors；2 個既有 Fast Refresh warnings。
- `npm run typecheck`：通過。
- `npm run build`：通過；1900 modules，保留既有 >500 kB chunk warning。
- `git diff --check`：通過。
- Backend、前端 API／types、package manifests：無 diff。
- Browser 實機目視尚未執行；依 PLAN 第 13 章覆蓋 Card Grid、Filter Drawer、Modal、照片 controls、light／dark 與各 viewport。

### Commit 與 Claude review

- 主章節 commit：`1f0839b ui：精簡車輛流程與照片文案`
- Claude findings：沒有新的可重現、使用者可觀察且在 v1.6 範圍內的 finding。
- Reviewer 殘餘風險：排序／設封面原測試只驗證控制存在、非 admin 整備支出結果與頁碼超界空狀態未被 assertion；均屬測試保護不足，不是 runtime finding。
- 處理：查證三項均可在本章以純測試補強；新增實際 API call、pending approval、頁碼超界文案與返回控制存在 assertions，不修改 runtime。
- Follow-up commits：`92d28aa test：補強車輛狀態與照片操作回歸`
- Follow-up 複核：沒有新的可重現、使用者可觀察且在 v1.6 範圍內的 finding；Claude 另以單檔連跑 6 次及全套測試確認無非同步 race。
- 零 finding gate：通過。
- 後續驗證：第 10 章統一檢查 visible label 的程式化關聯；第 13 章確認成交長句與照片 controls 在 320～390px 的換行及觸控可用性。

## 第 8 章：收支、客戶、Audit 與 Print

狀態：完成，主 commit 與 follow-up 均通過零 finding gate；紙本／PDF 肉眼檢查依 PLAN 原文延後至 13.2。

- 新增收支規則縮為「一般營運收支不綁車；單車收支必須選擇關聯車輛。」；category、vehicle、payload 與 idempotency 流程不變。
- 盤點時發現日期、分類、金額與資金帳戶原本只有表單底部 general error，與 PLAN 的 per-field error 契約不符；已將相同驗證規則改在對應欄位旁呈現，補上唯一 ID、`aria-invalid` 與有效的 `aria-describedby`。
- 收支列表不改 copy：active chips、頁碼超界／篩選無結果／無資料三態、審核狀態、核准／駁回、request sequence fencing、URL Filter 與三角色金額／帳戶遮蔽均保留。
- 客戶頁頭已於第 2 章完成精簡；本章移除 CustomerSelect 電話欄重複 placeholder，既有客戶選取時以可見短句說明電話來源，查無結果則縮為「填寫電話後將建立新客戶」。
- Audit 移除登入／新增／修改／刪除用途列舉，只保留「紀錄僅供查詢。」；異動欄位 empty state、請求路徑、IP 與 User-Agent 不變。
- Print 沒有明顯重複副標，因此不改 runtime；車輛建檔與成交結案的正式欄名、收支「說明」、金額總計、備註與簽名欄皆由測試保護。紙本／PDF 肉眼檢查明確承接至 PLAN 13.2，不提前勾選。
- 新增 11 個 presentation tests，覆蓋收支 per-field error、Filter／empty、審核操作、sales 遮蔽、CustomerSelect 狀態、客戶無結果／關聯／刪除錯誤、Audit 唯讀與請求資訊，以及兩張 Print 契約。

### 驗證

```text
npm test -- --run src/pages/money-entries/MoneyEntryPresentation.test.tsx src/pages/OperationsPresentation.test.tsx
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
git diff -- backend frontend/src/api frontend/src/types frontend/package.json frontend/package-lock.json
```

結果：

- 第 8 章直接測試：2 files／11 tests passed。
- `npm test`：24 files／130 tests passed。
- `npm run lint`：0 errors；2 個既有 Fast Refresh warnings。
- `npm run typecheck`：通過。
- `npm run build`：通過；1900 modules，保留既有 >500 kB chunk warning；測試不進 production bundle，runtime 文案、欄位錯誤與讀屏關聯使主 chunk 較第 7 章增加約 1.19 kB。
- `git diff --check`：通過。
- Backend、前端 API／types、package manifests：無 diff。
- Browser／紙本／PDF 實機目視尚未執行；依 PLAN 13.2 統一覆蓋。

### Commit 與 Claude review

- 主章節 commit：`e71755a ui：精簡收支客戶與稽核文案`
- Claude findings：
  - F1（Low）：CustomerSelect 移除電話 placeholder 後，可見的既有客戶電話來源沒有程式化關聯到 read-only 電話欄。有效；為短句加入唯一 ID，電話欄只在選取既有客戶時以 `aria-describedby` 引用，並補精確測試。
  - F2（Low）：Print 測試只驗證金額標籤，卻宣稱金額值受保護。有效；改為精確驗證收入／支出／毛利總計、收入明細與收購價。
- 額外測試維護：以 `afterEach` 還原 fake timers 與 spies，避免前置 assertion 失敗時污染後續測試。
- Follow-up commits：`da8acf1 fix：補足客戶讀屏與列印金額契約`
- Follow-up 複核：兩項 findings 均完整修復，沒有新的可重現、使用者可觀察且在 v1.6 範圍內的 finding；Claude 獨立重跑 24 files／130 tests、lint、typecheck 與 build，結果一致。
- 額外閉環：補上未選既有客戶時電話欄不得殘留 `aria-describedby` 的反向 assertion；PLAN 13.3 新增窄螢幕送出多欄錯誤後仍可感知與修正第一個錯誤的實機檢查。
- 零 finding gate：通過。
- 後續驗證：第 10 章擴大檢查全站 visible label 與 error 關聯；第 13 章確認寬表格、CustomerSelect listbox、Audit 展開列與兩張 Print 預覽。

## 第 9 章：全站狀態文案一致性

狀態：完成；第十一輪 Claude 複核零 finding gate 已通過。

- 全站 loading 維持「載入中／搜尋中／建立中／儲存中」等短狀態，不引入假數字或假資料；既有 Skeleton、responsive Card／Table 與 Grid 高度不變。
- Audit 原本在無任何資料時仍顯示「尚無符合條件」，是全站唯一混淆無資料與 Filter 無結果的正式列表；已拆成「尚無稽核紀錄」與「尚無符合條件的稽核紀錄」，後者才顯示清除 action。
- Per-field error 保持欄位旁；Login、Account、強制改密碼、車輛／收支／客戶新增、員工／資金帳戶 create/edit/reset 與車輛 Workflow Modal 的 general API error 統一移到各 form 頂部。
- General error 不改文字、API validation message、payload、idempotency 或成功流程；審查後統一改用 `FormAlert`，錯誤出現時以 assertive live region 宣告並聚焦，使長表單與 Mobile viewport 也能立即捲動至錯誤。
- 一般 Account CRUD success 維持短句；員工建立／重設密碼、Session 失效、重新登入與強制改密碼等有後果的結果保持完整。
- 薪資阻擋與非阻擋 warning、車輛條件式 warning、User self disabled reason 與 CustomerSelect read-only reason 均維持條件式呈現及可見／程式化關聯；正式 UI 沒有把 `title` 當唯一原因。
- 新增 4 個全站狀態 tests，覆蓋 ProtectedRoute loading／登出阻擋與 Audit 兩種空狀態；新增 `FormAlert` tests，並擴充 MoneyEntry 與 Vehicle tests，驗證 per-field error 先阻擋送出、general error 位於第一個欄位之前且出現時取得焦點。

### 驗證

```text
npm test -- --run src/components/FormAlert.test.tsx src/pages/StatusPresentation.test.tsx src/pages/Login.test.tsx src/pages/PasswordChangeRequired.test.tsx src/pages/account/Account.test.tsx src/pages/money-entries/MoneyEntryPresentation.test.tsx src/pages/vehicles/VehiclePresentation.test.tsx src/pages/users/UserList.test.tsx src/pages/cash-accounts/CashAccountList.test.tsx src/pages/OperationsPresentation.test.tsx src/pages/salary/SalaryPresentation.test.tsx
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
git diff -- backend frontend/src/api frontend/src/types frontend/package.json frontend/package-lock.json
```

結果：

- 狀態相關直接測試：11 files／69 tests passed。
- `npm test`：26 files／145 tests passed。
- `npm run lint`：0 errors；2 個既有 Fast Refresh warnings。
- `npm run typecheck`：通過。
- `npm run build`：通過；1901 modules，保留既有 >500 kB chunk warning。
- `git diff --check`：通過。
- Backend、前端 API／types、package manifests：無 diff。
- Browser 實機目視尚未執行；依 PLAN 第 13 章確認 general error 聚焦後的 Mobile 捲動、Modal 高度與 keyboard／touch 行為。

### Commit 與 Claude review

- 主章節 commit：`e196813 ui：統一全站狀態與錯誤呈現`
- Claude findings：
  - High：VehicleCreate 長表單的頂部錯誤可能在 Mobile viewport 外，且沒有 live announcement、focus 或 scroll。
  - Medium：MoneyEntry、Customer、User、CashAccount 與車輛 Workflow 表單有相同可感知性缺口。
  - Low：MoneyEntry 的 category error assertion 為恆真式檢查，狀態測試名稱也過度聲稱 keyboard 覆蓋。
- 處理：三項 finding 均可重現且在 v1.6 Accessibility 範圍內；新增共用 `FormAlert` 的即時宣告與聚焦，套用所有本章移動的 general form errors，並將 MoneyEntry 測試改為 field validation → API error 的兩階段斷言、校正測試名稱。
- 第一個 follow-up commit：`0b267df fix：讓表單錯誤可即時感知`
- Claude follow-up findings：
  - High：車輛 Workflow Modal 沒有 viewport 高度與捲動容器，聚焦錯誤仍可能在小螢幕外。
  - High：同步 client validation 重複產生相同訊息時，React state 不變會使 `FormAlert` effect 不重跑。
  - Medium：新增收支多欄 per-field error 沒有移動至第一個錯誤；屬第 10 章 Accessibility 契約，且已在第 13.3 章列有 320～390px 實測項目，本章不提前夾帶。
  - Medium：薪資三頁的共用載入／寫入錯誤未使用即時感知契約。
  - Low：`role="alert"`、assertive live region 與 focus 在特定真實 SR 組合可能重複朗讀；jsdom 無法重現，保留至第 13 章實機 SR 驗證，不以推測變更語意。
- 第二輪處理：Modal panel 增加 `100dvh` 高度上限與垂直捲動；`FormAlert` 新增 submit signal，Vehicle、User、CashAccount 的同步驗證每次送出都重新聚焦；薪資三頁保留共用 error state 與區塊位置並套用 `FormAlert`。新增同訊息重送與 Modal scrolling contract 測試。
- 第二個 follow-up commit：`e612fec fix：補強錯誤重送與行動版感知`
- Claude 第二輪 follow-up findings：
  - Medium：待補獎金歸屬的同步 validation 仍使用頁首 plain text，重複點擊沒有新回饋。
  - Medium/Low：SalaryPeriodDetail 載入失敗的 early return 繞過 `FormAlert`，SalaryPeriodList 也尚未納入。
  - Low：User、MoneyEntry、CashAccount 的 row action general error 與同頁表單／success status 不一致。
- 第三輪處理：待補獎金歸屬加入 save signal 與重送測試；薪資月份列表及 detail early-return 統一使用 `FormAlert`；三個 row action error 統一使用 `FormAlert`，MoneyEntry 核准／駁回先清除舊錯誤；Audit 與 Customer 列表載入錯誤一併統一，避免全站盤點再留相同 plain error。PLAN 13.2 新增真實 screen reader 對 live region／focus 重複朗讀的明確 gate。
- 第三個 follow-up commit：`1c70111 fix：補齊列表與薪資錯誤回饋`
- Claude 第三輪 follow-up findings：
  - High：`FormAlert` 預設聚焦使被動列表 reload failure 搶走搜尋／Filter 欄位焦點。
  - Medium：車輛照片刪除、設封面與排序的 action error 仍是照片 Grid 上方的 plain text，Mobile 下不可感知。
- 第四輪處理：`FormAlert` 改為 `focusOnShow` 明確 opt-in；被動載入失敗只保留 assertive live announcement，表單提交與主動 row action 才聚焦。混合載入／操作 error state 的 User、CashAccount、MoneyEntry 與薪資頁另記錄錯誤來源，避免被動 reload 搶焦點。照片三種 action 增加 attempt signal 並套用聚焦 `FormAlert`。新增「被動錯誤不搶搜尋焦點」與照片 action error 聚焦測試。
- 第四個 follow-up commit：`1ecd1ed fix：區分主動與被動錯誤焦點`
- Claude 第四輪 follow-up findings：
  - Medium：照片上傳與照片載入 error 仍是 plain text，與同面板其他 action error 契約不一致。
  - Medium：CustomerDetail 刪除失敗寫入整頁 load error，造成客戶內容與操作全部卸載、焦點掉回 body。
- 第五輪處理：照片上傳新增 attempt signal 並以聚焦 `FormAlert` 顯示，上傳重試沿用原 idempotency key；照片載入錯誤改為不聚焦的被動 `FormAlert`。客戶刪除改用獨立 delete error／attempt，失敗後保留完整客戶內容、返回列表與關聯資料，並聚焦具體錯誤；載入失敗維持被動不搶焦點。補強對應 role、focus 與內容保留測試。
- 第五個 follow-up commit：`c2bfd22 fix：補齊照片與客戶操作錯誤`
- Claude 第五輪 follow-up findings：
  - Medium：VehicleDetail 的 Workflow 操作成功後若 refresh 失敗，既有資料仍被 page-level early return 卸載。
  - Medium：CustomerDetail 儲存成功後若 refresh 失敗，仍會走同型 early return；前一輪只分離了 delete failure。
- 第六輪處理：兩個詳情頁都只在「尚無 detail 的首次載入失敗」early-return；已有資料時以不聚焦的 `FormAlert` 宣告 refresh failure 並保留狀態、操作與返回列表，成功 reload 會清除舊錯誤。新增車輛操作與客戶儲存成功、後續 refresh 失敗的內容保留測試。
- 第六個 follow-up commit：`ee65f7d fix：保留詳情頁重新載入前狀態`
- Claude 第六輪 follow-up findings：
  - Medium：照片 action 成功後若 list refresh 失敗，既有照片 Grid 仍被 `loadError` 閘門隱藏。
  - Medium：Vehicle／Customer 操作成功但 refresh 失敗仍沿用首次載入文案，使用者無法判斷操作已送出，可能重複高風險操作。
- 第七輪處理：照片已有資料時不因被動 refresh failure 隱藏 Grid 與管理按鈕；首次無資料失敗仍只顯示錯誤。Vehicle、Customer 與同型 Salary action refresh 改用「操作已送出／資料已儲存，但畫面可能不是最新；請重新整理後確認」口徑，初次載入仍保留原文案。新增照片 refresh failure 內容保留測試並更新兩個詳情頁 stale-data 文案斷言。
- 第七個 follow-up commit：`86b6d14 fix：標示操作後資料更新失敗`
- Claude 第七輪 follow-up findings：
  - Medium：照片上傳／刪除／設封面／排序成功後的 refresh failure 仍沿用首次照片載入文案，可能造成重複上傳或誤判不可逆操作。
  - Medium：收支核准／駁回成功後列表 refresh failure 仍顯示一般列表載入失敗，pending stale row 容易使管理員誤判審核未成功。
- 第八輪處理：`loadPhotos` 依首次載入或 action refresh 使用不同錯誤文案，成功 action 後明確提示操作已送出、照片可能不是最新；排序 action 本身失敗後的 rollback reload 仍保留一般載入文案。MoneyEntry 以 ref 記錄 review refresh 來源，核准／駁回成功後若 reload 失敗顯示「審核已送出，但列表可能不是最新；請重新整理後確認。」並保留 stale row 供使用者理解需重新整理。更新照片與 approved-only 高風險審核測試。
- 第八個 follow-up commit：`f38faf3 fix：區分審核與照片更新失敗`
- Claude 第八輪 follow-up findings：
  - Medium：首次載入失敗時，多個列表仍同時渲染「尚無資料」與新增 CTA，造成 error／empty state 混淆。
  - Medium：User、CashAccount、SalaryProfiles／Plans／PeriodList 與待補獎金歸屬的 action 成功後 refresh failure 仍沿用首次載入文案。
- 第九輪處理：MoneyEntry、User、Customer、CashAccount、Audit、Salary 三頁與待補獎金歸屬的 empty state 全部增加 `!error` gate，載入失敗不再宣稱資料為空。六個 action-refresh 頁面沿用 default initial error／明確 stale-data error 的 load 參數模式，成功 action 後提示操作已送出或資料已儲存、列表可能不是最新。擴充 Audit error-vs-empty 與 CashAccount action-refresh 測試。
- 第九個 follow-up commit：`9eb55da fix：統一載入失敗與空狀態`
- Claude 第九輪 follow-up findings：
  - Medium：CustomerSelect 搜尋 API 失敗仍顯示「查無相符客戶」，可能誤導使用者建立重複客戶。
  - Low：CommissionPlans 與 SalaryPeriodList 的 create action failure 被 `!error` 當成 load failure，原本空狀態一併消失。
- 第十輪處理：CustomerSelect 增加獨立 `searchError`，搜尋失敗顯示可見且可宣告的「客戶搜尋失敗」，不再同時宣稱查無結果；新搜尋、選取與改為新客戶時清除舊錯誤。兩個薪資頁以 `loadFailed` 區分 load failure 與 create failure：載入失敗隱藏假空狀態，建立失敗則保留原有空狀態與 CTA。新增搜尋失敗與薪資 create failure 回歸斷言。
- 第十個 follow-up commit：`4e23ad9 fix：區分搜尋與建立失敗狀態`
- Claude 第十輪 follow-up finding：
  - Low：薪資頁 `loadFailed` 在 create failure 時被清除；若先 load failure 再 create failure，假空狀態會重新出現。
- 第十一輪處理：`loadFailed` 改為「目前列表是否曾成功載入」；load 開始與 create failure 均不清除，只有 load success 才清除。新增 SalaryPeriodList 的 load reject → create reject 組合測試，確認兩次錯誤後仍不宣稱列表為空；CommissionPlans 使用相同 state contract。
- 第十一個 follow-up commit：`73fc32e fix：保留薪資列表載入失敗狀態`
- 第十一輪 Claude 複核：上一輪 finding 已完整關閉；沒有新的可重現、使用者可觀察且屬第 9 章範圍的 finding。Claude 獨立重跑 26 files／145 tests，並確認 backend、frontend API／types、package manifests 與 runtime dependency 均無 diff。
- 零 finding gate：通過。
- 後續驗證：第 10 章檢查所有狀態的語意／讀屏關聯；第 13 章以 keyboard、touch、320～1440px 與 light／dark 實測。

## 第 10 章：Accessibility 與語意回歸

狀態：完成；Claude follow-up 零 finding gate 已通過。

- 逐頁搜尋 101 個 input／select／textarea 與 148 個 heading、ARIA、live-region、described-by、label／title 使用點；確認 v1.6 刪除 help text 後沒有留下失效 `aria-describedby`。另辨識出 v1.5 起即存在、以 sibling visible label 呈現但未用 `htmlFor` 關聯的控制項；本章不擴張為全站表單重構，已在 PLAN 13.2 增列真實 screen reader 抽查，不把「visible label 保留」誤寫成已全面改善既有 programmatic association。
- 所有表單維持可見 label、required marker 與原生 `required`；placeholder 只保留搜尋／輸入提示，不作唯一欄位名稱。MoneyEntry 四個 per-field error 使用唯一 ID，並只在錯誤存在時加入 `aria-describedby`。
- 新增收支送出多欄錯誤後，會在 React commit 後聚焦 form 內第一個 `[aria-invalid="true"]` 欄位；焦點由 validation submit attempt 序號觸發，相同錯誤重送會重新聚焦，使用者修正欄位時不會再移動焦點。這是第 9 章明確移交的首錯可感知性修正，不改 validation、payload 或 API。
- Account／強制改密碼的 help 仍先於 error，described-by 順序為 help → error；UserList self restriction 仍由單一可見短句與四個 disabled controls 的 described-by 共同取得。
- 全站 page `h1`、section `h2`、Card Link／Button accessible name 與 icon-only control 的 `aria-label` 維持；UserList admin／非 admin 的兩個 `h1` 位於互斥 render 分支，不會同時出現在 DOM。
- Dashboard 三張圖維持 `role="img"` 及包含期間、指標、金額口徑與單位的 accessible name；ThemeToggle 的 `aria-label` 與可見 title 同義，title 不是唯一名稱。
- 新增 MobileFilterDrawer contract test，驗證 dialog title／`aria-modal`、開啟後關閉按鈕取得焦點、Escape close 與 trigger focus return；既有 Tab trap 程式碼未改。Vehicle Workflow 與 Salary adjustment Modal 的既有語意／focus 行為不是 v1.6 新增或回歸，本章不改互動平台，依 PLAN 13.2 keyboard 與 13.3 Modal 可操作項目實測。
- Dynamic warning 維持條件式顯示且不主動搶 focus；主動 general error 與被動 loading error 的 focus 分流沿用第 9 章已通過契約。真實 screen reader 重複朗讀與 Mobile viewport 目視依 PLAN 13.2／13.3 執行。

### 驗證

```text
npm test -- --run src/components/MobileFilterDrawer.test.tsx src/pages/money-entries/MoneyEntryPresentation.test.tsx src/pages/PasswordChangeRequired.test.tsx src/pages/account/Account.test.tsx src/pages/users/UserList.test.tsx src/pages/OperationsPresentation.test.tsx src/pages/Dashboard.test.tsx src/layouts/AppLayout.test.tsx
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
git diff -- backend frontend/src/api frontend/src/types frontend/package.json frontend/package-lock.json
```

結果：

- Accessibility 直接測試：8 files／45 tests passed。
- `npm test`：27 files／146 tests passed。
- `npm run lint`：0 errors；2 個既有 Fast Refresh warnings。
- `npm run typecheck`：通過。
- `npm run build`：通過；1901 modules，保留既有 >500 kB chunk warning。
- `git diff --check`：通過。
- Backend、前端 API／types、package manifests：無 diff。
- Browser／真實 screen reader 尚未執行；PLAN 第 13 章保留 keyboard、touch、SR、320～1440px、light／dark 與 Modal／Drawer 實測。

### Commit 與 Claude review

- 主章節 commit：`01f1452 a11y：完成全站語意與焦點回歸`
- Claude findings：
  - Medium：MoneyEntry 聚焦 effect 依賴 `fieldErrors` 物件；使用者修正一個欄位時會把焦點移到下一個錯誤，可能只輸入第一個字元。
- 處理：finding 可重現且屬第 10 章；改以 validation submit attempt 驅動首錯聚焦，相同錯誤重送仍會聚焦，onChange 清除 field error 不再觸發焦點移動。測試新增輸入完整 `1000` 後焦點與值都留在金額欄的斷言。
- Claude 非 finding 殘留：v1.5 既有 sibling visible labels 與兩類既有 Modal 沒有因 v1.6 copy cleanup 退化；不擴張成全站重構，已明確納入 PLAN 13.2／13.3 真實 SR、keyboard 與 Modal 可操作實測。
- Follow-up commit：`12672c7 fix：限制欄位錯誤聚焦時機`
- Claude follow-up 複核：上一輪 Medium 已完整關閉；沒有新的可重現、使用者可觀察且屬第 10 章範圍的 finding。剩餘 sibling labels、既有 Modal 與真實 SR／Mobile 風險均由 PLAN 第 13 章未勾項目承接。
- 零 finding gate：通過。

## 第 11 章：Frontend Automated Verification

狀態：完成；Claude High 零 finding gate 已通過。

- 修正第 10 章 closure 複核指出的盤點統計：非測試 TSX 共 101 個 input／select／textarea；不影響已通過的實質 Accessibility 契約。
- `npm test` 全套 27 files／146 tests 通過；Dashboard、Account／Auth、User、Salary warning、Vehicle／MoneyEntry／Filter、App config 與 App Layout 均包含在 full suite，沒有為測試恢復已判定刪除的副標或教學文案。
- lint 仍只有 `useAuth.tsx` 與 `useTheme.tsx` 兩個 v1.5 已存在的 Fast Refresh warnings；v1.6 沒有新增 warning。
- 以暫時 detached worktree 在 `539599d`／`v1.5-smoke-passed` 共用相同 `node_modules` 重建 production baseline，完成後已移除暫存 worktree：
  - v1.5：JS 523.68 kB／gzip 140.14 kB；CSS 33.07 kB／gzip 7.27 kB；1900 modules。
  - v1.6：JS 526.40 kB／gzip 140.72 kB；CSS 33.59 kB／gzip 7.32 kB；1901 modules。
  - 差異：JS +2.72 kB／gzip +0.58 kB；CSS +0.52 kB／gzip +0.05 kB。主要來自 `FormAlert` 與既有狀態／Accessibility 契約，沒有大幅 bundle growth；兩版都保留相同 >500 kB chunk warning。
- `539599d..HEAD` 靜態 scope audit：Backend、database、routes、migration、frontend API／types、package manifests、frontend routes／`App.tsx` 均無 diff。
- Runtime 新增檔只有共用 `FormAlert.tsx`；其他新檔為 tests／docs。Dashboard 只調整既有 Card 與 chart copy／ARIA description，沒有新 KPI、圖表或報表。

### 驗證

```text
npm test
npm run lint
npm run typecheck
npm run build
git diff --check
git diff --exit-code 539599d..HEAD -- backend
git diff --exit-code 539599d..HEAD -- backend/database
git diff --exit-code 539599d..HEAD -- backend/routes
git diff --exit-code 539599d..HEAD -- frontend/src/api frontend/src/types
git diff --exit-code 539599d..HEAD -- frontend/package.json frontend/package-lock.json
git diff --exit-code 539599d..HEAD -- frontend/src/routes frontend/src/App.tsx
```

結果：

- Frontend tests：27 files／146 tests passed。
- lint：0 errors；2 個既有 warnings，v1.6 新增 0。
- typecheck：通過。
- production build：通過；1901 modules，JS 526.40 kB／gzip 140.72 kB。
- `git diff --check` 與所有 scope audit：通過。

### Commit 與 Claude review

- 主章節 commit：`299d238 test：完成 v1.6 前端完整驗證`
- Claude findings：沒有新的可重現、可觀察、範圍內 finding。Claude 獨立重跑 27 files／146 tests、lint、強制 typecheck、兩版 production build、101 個控制項盤點及全部 scope audit，結果與本章紀錄一致。
- Claude 非 finding 觀察：
  - `CustomerCreate.tsx` 沒有頁面層直接 render test，但本次變更的 `FormAlert focusOnShow` 契約已由共用元件測試覆蓋；PLAN 11.1 未宣稱客戶頁直接覆蓋，不擴張測試範圍。
  - PLAN 11.3 checkbox 沿用不帶 baseline 的簡寫，實際驗證與本章命令紀錄均明確使用 `539599d..HEAD`，證據有效。
- Follow-up commits：無。
- 零 finding gate：通過。

## 第 12 章：Backend Regression Baseline

狀態：完成；Claude High 零 finding gate 已通過。

- `php artisan test` 完整回歸為 633 tests；616 passed、17 skipped、3170 assertions，與 `v1.5-smoke-passed` 的最終 baseline 完全一致，沒有新增或消失的測試、skip 或 assertion。
- 第二次以 `./vendor/bin/phpunit --display-skipped` 執行仍為相同結果；本環境的 PHPUnit wrapper 只輸出摘要，因此另以測試原始碼、既有 v1.5 smoke report 與 `539599d..HEAD` diff 交叉核對 17 個 skipped：
  - `VehicleCreateMysqlConcurrencyTest` 4 個。
  - `SalarySettingsMysqlConcurrencyTest` 6 個。
  - `TimezoneMysqlIntegrationTest` 3 個。
  - `UserAccountMysqlIntegrationTest` 2 個。
  - `VehicleFinalPaymentMysqlConcurrencyTest` 1 個。
  - `VehicleWorkflowTest` 1 個已由新版雙行程競態測試取代的舊測試。
- 前五類都需要真實 MySQL／MariaDB、明確環境旗標及可拋棄 schema；競態案例另需要 pcntl／posix 與 socket barrier。一般 SQLite memory full suite 沒有把這些案例寫成通過。
- `git diff --exit-code 539599d..HEAD -- backend` 通過；v1.6 沒有 Backend runtime、test、schema、migration、timezone、transaction、API 或 route diff，environment gate 本身也未變。
- 因 v1.6 僅有前端 copy／presentation runtime diff，且 MariaDB 專用測試會對明確允許的可拋棄 schema 執行 `migrate:fresh`，本章依 PLAN 不重跑 MariaDB 競態與時區整合；沿用截至 v1.5 封板時已存在、分散於 v1.3～v1.5 smoke report 的專用整合證據，不宣稱本次通過。
- Full suite 沒有基準失敗，因此不需要以環境問題例外處理，也沒有用 v1.6 文案變更掩蓋 Backend 問題。

### 驗證

```text
cd backend && php artisan test
cd backend && ./vendor/bin/phpunit --display-skipped
git diff --exit-code 539599d..HEAD -- backend
git diff --check
```

結果：

- 兩次 Backend full suite：633 tests；616 passed、17 skipped、3170 assertions。
- 17 個 skipped：全部為 v1.5 已存在的 environment-gated／已取代測試；本次未寫成通過。
- Backend 與全工作樹靜態 diff check：通過。

### Commit 與 Claude review

- 主章節 commit：`2b0e6bf test：建立 v1.6 後端回歸基準`
- Claude findings：沒有新的可重現、可觀察、範圍內 finding。Claude 以不覆寫 result cache 的方式獨立重跑 full suite，並由 cache、原始碼與 baseline diff 複核全部數字及 17 個 skip。
- Claude 非 finding 觀察：
  - MariaDB 專用證據分散在 v1.3～v1.5；本章已明確寫成沿用「截至 v1.5 封板時已存在」的證據，不暗示 v1.5 曾重跑全部 16 個 gated tests。
  - PLAN 的 environment-gated 簡寫未涵蓋第 17 個無條件淘汰測試；progress 已用「environment-gated／已取代」正確區分。
  - 舊 `VehicleWorkflowTest` skip 後仍有不可達程式碼，但自 `539599d` 即存在且 v1.6 Backend 零 diff，不在本版處理。
- Follow-up commits：無。
- 零 finding gate：通過。

## 第 13 章：Browser Engineering Smoke

狀態：完成；Claude High 第三輪 closure 零 finding gate 已通過。

- 建立只允許 testing SQLite `/tmp` 路徑的專用 fixture，涵蓋三角色、
  Dashboard 有值／零值、approved／pending／rejected 收支、不同車況及
  draft／confirmed／paid 薪資，不使用前端假資料。
- 以 Google Chrome for Testing 151.0.7922.47、真實 Laravel API 與精確
  Chrome CDP viewport 完成 32 項 WebDriver 工程 smoke；涵蓋
  1440／768／390／375／320px、light／dark、三角色、主要路由、
  keyboard-only、狀態提示、兩份 PDF、Drawer／Modal、表格捲動與
  Safe Area，結果 32/32 通過。
- 以真實 Orca 46.1、Xvfb 與非 headless Chrome 抽查登入 general error、
  MoneyEntry field error 及 CashAccount sibling visible label；三段
  `SPEECH OUTPUT` 各出現一次，沒有重複朗讀。
- Browser finding 1：Vehicle workflow 與 Salary adjustment Modal 缺少
  dialog semantics、Escape、Tab containment、scroll lock 與 focus return。
  新增共用 `useDialogFocus` 並補測試。
- Browser finding 2：主動 focus 的 `FormAlert` 同時 assertive announce，
  Orca 會重複朗讀。focus 模式改為 `aria-live="off"`；被動錯誤仍 assertive。
- Browser finding 3：CashAccount 建立／編輯欄位只有 sibling visible
  label。補上原生 `htmlFor`／`id` 關聯與 accessible-name 測試。
- Browser finding 4：MoneyEntry 首錯立即 focus 時 AT-SPI 尚未穩定，
  造成欄位錯誤重複朗讀。延後 100ms 聚焦；Vitest 與 Orca 均通過。
- Claude High F1（Medium）：CustomerSelect listbox 的 Escape 與新 dialog
  listener 衝突，會關閉整張 Reserve Modal 並遺失輸入。有效；dialog
  listener 改為尊重 `defaultPrevented`，新增第一次 Escape 收 listbox、
  第二次關 Modal 與 focus return 的回歸測試。
- Claude High F2（Medium）：將一次 sibling label 抽查擴張為 MoneyEntry、
  User 與全部表單修正，超出 PLAN 13.2「抽查」及本章實際宣稱；MoneyEntry
  被抽查的分類欄位本來已有原生關聯，User create form 本章未開啟或宣稱
  全面通過，因此不接受全站擴張。但其中新納入 focus trap 的 Vehicle
  workflow Modal 確屬本章範圍，已補 `Field`／`CashAccountField` 及三類
  workflow 直接欄位的原生 label 關聯與測試。
- Claude High F3（Medium）：Orca speech count 原由 report 外 shell
  `grep` 取得，腳本本身不能重現。有效；腳本新增 `ORCA_DEBUG_LOG`，
  直接輸出並 assert 三個 speech count 都是 1，正式重跑通過。
- Claude High F4（Medium）：Manager 入口負向斷言使用不存在的「員工管理」
  字串。有效；改為比對「員工/帳號管理」「薪資結算」「稽核紀錄」及
  `/api/users` 403，正式重跑通過。
- Claude High F5（Low～Medium）：1440px 除 Dashboard 外未逐頁量測
  light／dark overflow。有效；九個主要頁面逐主題量 hierarchy 與
  overflow，正式重跑通過。
- Claude High F6（Low）：Safe Area 條件無法證明 inset CSS。有效；改用
  Chrome CDP 注入 top 20px／bottom 34px，驗證 computed header／main
  padding 與 overflow。
- Claude High F7（Low）：刪副標後空白只有 overflow 證據。有效；新增
  header／action bounding boxes 與第一段內容間距（不超過 96px）量測，
  所有 Desktop／Mobile 主要頁面、light／dark 正式重跑通過。
- 第一輪 follow-up commit：`18d3f99 fix：修正工程 Smoke 審查問題`
- Claude High closure F1（Medium）：ListModal「銷售備註」仍是新 focus
  trap 範圍內漏掉的 orphan label。有效；補原生關聯與 accessible-name test。
- Claude High closure F2（Medium）：CustomerSelect 空 query 時沒有可收起
  的 listbox，卻仍 prevent Escape。有效；guard 改依 `showListbox`，
  回歸測試同時覆蓋空 query 直接關 Modal、有 query 先收 listbox。
- Claude High closure F3（Low）：hierarchy helper 在 Account 與 Salary
  Detail 沒涵蓋真正 top-level action。有效；改由 `main.app-main` page root
  選取包含 h1 的可見 top-level header 與下一段內容；後續複核確認
  Account 舊 helper 原已涵蓋內容間距，真正新增的是 Salary 返回 action
  的量測；正式 Chrome 32/32 重跑通過。
- 完整證據記錄於 `docs/v1.6-phase-smoke-report.md`。專用服務已停止，
  SQLite、Chrome、Orca prefs／debug 與其他 `/tmp/erpv2-v16-*`
  暫存已刪除；既有開發資料與服務未變更。

### 驗證

```text
SMOKE_BROWSER=chrome ... python3 docs/smoke/v1.6-phase13-webdriver.py
DISPLAY=:96 SMOKE_BROWSER=chrome ... python3 docs/smoke/v1.6-phase13-orca.py
cd frontend && npm test
cd frontend && npm run lint
cd frontend && npm run typecheck
cd frontend && npm run build
git diff --check
```

結果：

- Chrome WebDriver：32 checks passed。
- Orca speech output：登入錯誤 1 次、欄位錯誤 1 次、帳戶名稱 1 次。
- Frontend tests：27 files／150 tests passed。
- lint：0 errors；2 個既有 Fast Refresh warnings。
- typecheck：通過。
- production build：通過；1902 modules，JS 528.79 kB／gzip 141.42 kB。
- `git diff --check`：通過。
- Backend、API、schema、Business Logic、Workflow、權限與 dependency：
  無變更。

### Commit 與 Claude review

- 主章節 commit：`c109221 test：完成 v1.6 工程 Browser Smoke`
- Claude findings：7 項；6 項有效並已修正，1 項只接受 Vehicle
  workflow Modal 的範圍內部分，其餘全站擴張以 PLAN 與實際抽查證據退回。
- 第一輪 Follow-up commit：`18d3f99 fix：修正工程 Smoke 審查問題`
- Claude closure findings：3 項，均有效且已完成最小修正與完整回歸。
- 第二輪 Follow-up commit：`35e1669 fix：關閉 Browser Smoke 複核問題`
- Claude 第三輪 closure：zero finding。上一輪三項均已關閉；Claude
  獨立重跑 27 files／150 tests、lint，並靜態複核 32 個 smoke checks、
  bundle artifact 數字、Backend／API／schema／dependency 零 diff 與
  PLAN 13 全勾、14／15 未勾，沒有新的可重現、使用者可觀察、範圍內 finding。
- 非 finding：Account 舊 helper 本來已 fallback 至 h1／content，因此文件
  改為「確認仍涵蓋」；login retry 只在仍停留 `/login` 時重送一次，第二次
  失敗仍會 assertion，不掩蓋硬失敗；iPhone Safari／VoiceOver 與主觀視覺
  比例仍依 PLAN 第 15 章由使用者 manual smoke。
- 零 finding gate：通過。

## 第 14 章：Adversarial Review

狀態：完成；Claude Opus 5 High 全版 runtime zero finding，文件 follow-up
closure zero finding gate 已通過。

- `539599d..HEAD` 共 55 個檔案：Frontend runtime 27、Frontend test 14，
  其餘為企劃／PLAN／進度／工程 smoke 文件。沒有刪除檔案。
- Runtime 新增檔只有 `FormAlert.tsx` 與 `useDialogFocus.ts`；沒有新增頁面、
  route、API、types、Backend、schema、migration、package dependency、
  KPI、圖表、報表、通知、onboarding、tooltip 平台或平行 UI 系統。
- 逐一比對所有刪除的高風險文字與現行 render/test：Dashboard 仍保留
  approved-only、完整月份、成交日期與每日期末餘額；Vehicle 仍保留
  成交月份、收款日期、薪資鎖定、收購價與不可逆照片刪除；Salary 仍保留
  確認鎖定、發薪帳戶／日期與唯讀後果；User／Account 仍保留重設密碼、
  既有 Session 失效、重新登入與不可逆刪除後果。
- 逐項檢查新增／修改的 `aria-describedby`：CustomerSelect selected hint、
  Account username/password help→error、PasswordChangeRequired help→error、
  MoneyEntry 四個 field error、User self restriction 均有存在中的唯一 ID
  與直接 regression tests，沒有失效關聯。
- User self disabled controls 以單一可見短句及 `aria-describedby` 提供原因；
  測試確認 role control 不依賴 `title`，其他 disabled controls 同樣取得
  可見原因。
- 第 13 章已對 1440／768／390／375／320px、light／dark、條件式 draft
  warning、form error、Modal／Drawer 與主要頁面逐頁量 hierarchy／overflow，
  沒有因條件式文案造成 Mobile overflow。
- 補完 PLAN 8.5：以真實 fixture 重新產生建檔與成交結案 PDF，轉成 PNG
  逐頁肉眼檢查。兩份皆為單頁，標題、欄位、表格、金額摘要、備註與簽名區
  無裁切、重疊或錯位；所有暫存 artifacts 已刪除。
- Reviewer 邊界已鎖定：finding 必須有頁面、缺失資訊、重現路徑與可觀察
  影響；不接受 tooltip 搬運、個人文案偏好、整站視覺改版或 Backend／schema
  擴張。

### 驗證與範圍稽核

```text
git diff --stat 539599d..HEAD
git diff --name-status 539599d..HEAD
git diff --exit-code 539599d..HEAD -- backend backend/database backend/routes
git diff --exit-code 539599d..HEAD -- frontend/src/api frontend/src/types
git diff --exit-code 539599d..HEAD -- frontend/package.json frontend/package-lock.json
git diff --exit-code 539599d..HEAD -- frontend/src/App.tsx
git diff -U0 539599d..HEAD -- frontend/src
git diff --check
```

結果：

- Backend／schema／routes、Frontend API／types、package manifests、
  `App.tsx`：零 diff。
- 高風險文案、ARIA 關聯、disabled reason 與 Mobile 工程證據：均已逐項
  回到 runtime 與 tests 查證。
- `git diff --check`：通過。
- 主章節 commit：`08b09d7 docs：建立 v1.6 完整對立審查基準`

### Claude High 全版審查

- 範圍：`539599d..08b09d7` 完整 54 commits／55 files，不只審查最後文件。
- Runtime verdict：zero finding。Claude 獨立查證 Dashboard backend 口徑、
  所有高風險文案、16 個 static `htmlFor`、13 處 `aria-describedby`、
  disabled reason、條件式狀態、CustomerSelect／Modal Escape、scope diff
  與 smoke fixture 防護，沒有新的可重現、可觀察、範圍內 runtime finding。
- Low 文件 finding：`08b09d7` 在 Claude 全版審查執行前就把 PLAN
  14.1／14.2 勾選，與 progress 的「待接受 Claude 審查」矛盾，混淆
  Codex 自審與獨立 review gate。有效；14.1 改名為 Codex 自審與 Claude
  Review 共同目標，只有本輪完成後才維持已勾狀態；14.2 改為不計完成度的
  Reviewer 契約條列。14.3 逐項查證／最小修正／驗證已完成；本輪只有文件
  finding，因此沒有 runtime regression test。
- Claude 獨立驗證：27 files／150 tests passed；lint 0 errors／2 個既有
  warnings；typecheck 通過；build 1902 modules、JS 528.79 kB／gzip
  141.42 kB、CSS 33.59 kB／gzip 7.32 kB。
- 殘餘人工風險：Dashboard KPI Grid 主觀列高、Modal submitting 時 Escape
  接受度與真實 iPhone Safari／VoiceOver，明確帶入第 15 章 manual smoke。
- Follow-up commit：`e20d2e0 docs：修正第 14 章審查閘門紀錄`
- Claude follow-up closure：上一輪 Low 已完整關閉；14.1 只在 Codex
  自審與 Claude review 都完成後維持已勾，14.2 已改為不產生完成度訊號的
  reviewer 契約，14.3 明確記錄文件 finding 不適用 runtime regression
  test。第 15／16 章仍未勾，沒有提前宣稱。
- 零 finding gate：通過。

## 第 15 章：使用者 Browser Manual Smoke

狀態：等待使用者以 Desktop Chrome、真實 iPhone Safari／VoiceOver 與
實際角色完成 manual smoke；本章所有 PLAN checkbox 保持未勾。

- 建立 `docs/v1.6-manual-smoke-checklist.md`，涵蓋 Dashboard 主觀資訊層級、
  常用車輛／收支／客戶／員工／帳號／薪資／列印流程、manager／sales
  權限抽查、真實 iPhone responsive／Safe Area 與 VoiceOver。
- 特別承接第 14 章殘餘人工風險：Dashboard KPI Grid 主觀列高、
  Modal submitting 時 Escape 接受度、真實 Safari／VoiceOver 行為。
- 清單提供 `通過／失敗／受阻／未測` 與裝置、角色、重現步驟、實際／預期
  結果回報格式；截圖只作輔助。
- 收到使用者實際結果前，不勾 PLAN 第 15 章、不宣稱 v1.6 完成、不進入
  第 16～18 章、不建立 tag、不 push。

### Commit 與 Claude review

- 清單 commit：`65ff489 docs：提供 v1.6 使用者 Smoke 清單`
- Claude High findings：
  - Medium：未明確禁止為查看 confirmed／paid 文案而真的確認／發薪，
    且員工刪除可能引導不可逆操作。有效；改為只檢視既有狀態、無資料填
    `未測`，刪除只檢視確認後取消，停用只用可恢復測試帳號並還原。
  - Medium：漏列 CommissionPlans、SalaryProfiles、待補獎金歸屬、
    CashAccount、Audit 與強制改密碼等 v1.6 改動頁。有效；補逐頁項目與
    安全操作邊界。
  - Low～Medium：沒有受測 runtime／build 基準。有效；記錄 runtime
    `35e1669`，要求在最新 main 重新 build／重啟並回報 HEAD。
  - Low：三角色只能列名稱，不能各自回報通過／未測。有效；改為 admin、
    manager、sales 三行獨立狀態。
- Follow-up commit：`4a6cbf1 docs：修正 v1.6 使用者 Smoke 清單`
- Claude High follow-up findings：
  - Medium：常用車輛流程會建立不可回復的 sold 車輛與
    `vehicle_workflow` 收支，永久影響資金帳戶、Dashboard 與當月薪資
    draft，原本只有籠統的測試環境前提。有效；明確限定只能在可整庫重置
    的測試資料庫走完整流程，否則只檢視既有資料並將無法安全驗證項目填
    `未測`。
  - Medium：`FormAlert focusOnShow` 在真實 iPhone Safari 可能收起軟鍵盤，
    原清單只驗證錯誤可見與 VoiceOver 朗讀，未驗證回欄修正體驗；登入頁
    亦沒有獨立項目。有效；新增 Desktop 登入與 Mobile 錯誤搶焦點後重試
    項目。
  - Low：失敗回報欄位只能容納一個問題，且無法定位 checkbox。有效；改為
    每個問題複製一份並記錄清單區段與項次。
- Second follow-up commit：`9f68a6a docs：補強 v1.6 人工 Smoke 安全邊界`
- Claude High second follow-up findings：
  - Medium：A3 已限制完整成交只能在可整庫重置環境執行，但 A4 仍可能
    引導 admin 建立立即 approved 且不可刪除的正式收支，或執行不可逆的
    核准／駁回。有效；A4 明列正式餘額、毛利與薪資影響，否則只檢視既有
    狀態並填 `未測`。
  - Low～Medium：重設密碼未限制測試帳號，可能讓真實員工立即失效且舊
    密碼不可復原。有效；限定只對可恢復的測試帳號操作。
  - Low：工程 light／dark smoke 只驗幾何，未承接 warning／error 等語意
    狀態的主觀對比。有效；最終主觀判斷補兩種主題的具體抽查項目。
  - Low：薪資草稿建立後沒有刪除 API，原項目未限制只檢視既有資料。有效；
    改為只檢視既有草稿，只有可整庫重置環境才建立，否則填 `未測`。
- Third follow-up commit：`fef3c8d docs：補齊 v1.6 Smoke 不可逆操作說明`
- Claude closure：2026-07-29 以 Opus 5／High／唯讀 plan mode 執行兩次，
  均在產生審查結論前由 Claude CLI 回報 `monthly spend limit`。不得視為
  zero finding；待額度可用後須重新審查 `65ff489..HEAD`，通過前維持第
  15 章與後續 PLAN checkbox 未勾。
- 2026-07-30 Claude High closure findings：
  - Medium：B 節要求 manager 完成車輛／收支流程，但未承接 A3／A4 的
    不可逆邊界，可能建立無法刪除的 pending 流程收支。有效；角色抽查改為
    以既有畫面與文案理解為主，只有可整庫重置環境才實際送出。
  - Medium：資金帳戶只禁止實際刪除，仍可能建立／修改期初餘額或類型，
    直接污染 Dashboard 現金口徑。有效；改為只檢視表單與確認文案，只有
    可整庫重置環境才實際建立或修改。
  - Low：manager／sales 測試帳號準備與帳號刪除限制互相衝突，可能導致
    角色抽查跳過或殘留可登入帳號。有效；優先使用既有專用測試帳號，否則
    只在可整庫重置環境新建；自建帳號避免業務關聯並於測後刪除，若已產生
    關聯則停用並回報。
- Fourth follow-up commit：`58a1636 docs：修正 v1.6 角色與帳戶 Smoke 邊界`
- Claude High fourth review findings：
  - Low～Medium：Desktop、Mobile、VoiceOver 與雙主題都要求觸發登入錯誤，
    可能撞上既有 identifier＋IP 或 account limiter，誤判成 v1.6 缺陷。
    有效；限制每個情境只刻意失敗一次、同帳號與 IP 一分鐘內不超過四次，
    並說明 limiter 到期時間與判讀方式。
  - Low～Medium：車輛列表與收支列表都是 v1.6 明列改動的高頻頁面，但清單
    沒有驗證刪除副標後的用途理解及 empty／no-result／page-out-of-range
    差異。有效；各補一個純篩選、唯讀的人工驗收項目。
- Fifth follow-up commit：`63cb782 docs：補齊 v1.6 列表與登入 Smoke 指引`
- Claude High fifth review findings：
  - Low～Medium：薪資月份列表是 v1.6 刪除副標並調整 empty／load-failed
    呈現的 admin 入口，但 A5 只有薪資明細項目。有效；補一個不建立草稿的
    唯讀項目，驗證用途、既有月份入口、空狀態下一步與載入失敗區分。
  - Low：進度 ledger 尚未把已完成的第四輪修正連到 `58a1636`。有效；依
    實際 Git history 回填，並同步記錄第五輪修正 `63cb782`。
- Sixth follow-up commit：`790f946 docs：補齊 v1.6 薪資列表 Smoke 項目`
- Claude High sixth review findings：
  - Low～Medium：A3 未開啟 admin「補登／修正收購價」Modal，無法主觀確認
    收購價影響毛利與獎金、薪資鎖定後不可修改等必留高風險文案。有效；
    補開啟後取消、不送出的唯讀項目，並包含鎖定時按鈕不出現的可見原因。
  - Low：車輛與收支列表的全空狀態在已有資料的環境無法透過篩選觸發，
    原項目沒有 `未測` fallback。有效；明列只在可整庫重置環境觀察，否則
    該子項填 `未測`。
  - Low：客戶建立是唯一尚未提供清理／關聯限制的建立型操作，綁車後即無法
    刪除。有效；限定建立未綁車測試客戶並於測後刪除，若意外綁定則回報並
    保留。
- Seventh follow-up commit：`cf728fd docs：補齊 v1.6 收購價與空狀態 Smoke 邊界`
- Claude final closure：2026-07-30 以 Opus 5／High／唯讀 plan mode 執行兩次，
  均在產生審查結論前由 Claude CLI 回報 `monthly spend limit`。不得視為
  zero finding；待額度可用後重新審查 `65ff489..HEAD`。
- 2026-07-30 Claude High resumed closure findings：
  - Medium：員工薪資設定與待補獎金歸屬缺少不送出邊界，後者儲存後會從
    待補清單消失且 UI 無法改回，並影響薪資獎金。有效；兩者均改為只檢視，
    歸屬只有可整庫重置環境才能送出。
  - Low～Medium：「此頁沒有資料」只能透過超出最後一頁的 URL 觸發，原清單
    沒有觸發方式或 `未測` fallback。有效；車輛與收支列表均補 `?page=999`
    唯讀步驟。
  - Low：獎金方案的已使用／未使用及收購價鎖定狀態可能缺少對應既有資料。
    有效；各子項補 `未測` fallback。
  - Low：收購價 Modal 實際控制名稱是「關閉」，不是「取消」。有效；改為
    「關閉」或 Escape，並維持不要送出。
  - Low：v1.6 新增的 Modal focus trap 未由自動測試或人工清單驗證 Tab
    containment。有效；車輛流程與薪資加扣項 Modal 各補 Tab／Shift+Tab
    循環且不送出的鍵盤項目。
- Eighth follow-up commit：`8d8a7c1 docs：補齊 v1.6 薪資寫入與鍵盤 Smoke 邊界`
- Claude High eighth review findings：
  - Low：薪資加扣項 Modal 的控制名稱是「取消／新增」，清單寫成通用的
    「關閉／送出」。有效；改用 runtime 實際名稱。
  - Low：薪資加扣項只在既有 draft 可開啟，缺少資料時沒有出口。有效；
    補「沒有既有 draft 請填未測」。
  - Low：focusable selector 排除 disabled 控制，而兩類 Modal 的送出按鈕在
    必填未完成時預設 disabled，原清單卻要求 Tab 必須抵達。有效；說明此
    狀態屬預期，可先填測試值啟用後只檢查焦點，禁止送出。
- Ninth follow-up commit：待建立。
- Claude resumed final closure：待執行。
- 使用者結果：等待回報。
