# ERPV2 v1.6 企劃書：介面文案精簡與資訊層級優化

## 1. 版本定位

v1.1～v1.5 的既定功能均已完成並封板。ERPV2 目前已能支援小型中古車行的主要營運流程，現階段沒有確認需要新增的商業功能。

v1.6 不新增功能，而是針對現有後台進行一輪小型、完整、可驗收的 Presentation Layer 收斂：

```text
移除重複教學文字
→ 縮短常駐輔助文案
→ 保留真正影響決策的規則
→ 只在例外、風險或錯誤發生時顯示說明
→ 讓日常操作畫面更安靜、更直接
```

正式版本名稱：

> ERPV2 v1.6：介面文案精簡與資訊層級優化

本版本核心原則：

> 正常操作時保持安靜；只有遇到例外、限制、風險或錯誤時才說明。

---

## 2. 問題背景

目前 UI 的主要問題不是資訊不足，而是部分頁面同時存在過多層級的說明：

```text
頁面標題
→ 頁面副標
→ 區塊副標
→ 卡片說明
→ 欄位 help text
→ 操作旁再補一段教學
```

當這些文字表達相同或不言自明的資訊時，會產生以下問題：

- 重要資訊與一般教學文字難以區分。
- Dashboard 與表單在 Mobile 上顯得擁擠。
- 熟悉系統後，使用者仍必須每天略過相同說明。
- 真正重要的金額口徑、不可逆後果與警告反而被大量灰色文字稀釋。
- 畫面容易呈現為操作說明書，而不是日常營運工具。

目前已盤點出較明顯的區域包括：

- Dashboard 區塊副標與 KPI Card description 重複。
- 「我的帳號」username 與密碼規則一次常駐顯示過多細節。
- 員工／帳號管理頁重複說明頁面用途與按鈕操作方式。
- 薪資頁面同時存在一般資訊、狀態提示與高風險警告，視覺權重不夠清楚。
- 新增收支、車輛流程與部分 Modal 以長句說明原本可以由欄位、狀態或驗證表達的規則。
- Audit、Customer、Cash Account 等頁面仍存在部分「這個頁面是做什麼」型副標。

v1.6 要處理的是整體文案層級，而不是只刪除幾句使用者當下看到的文字。

---

## 3. v1.6 目標

v1.6 必須達成：

1. 系統性盤點所有使用者可見的常駐副標、help text、說明句、提示與 warning。
2. 將文案分類為：刪除、縮短、條件式顯示、必須保留、無障礙專用。
3. 移除與頁面標題、欄位名稱、按鈕或狀態重複的說明。
4. 將高風險規則與一般教學文字分開，避免重要警告被稀釋。
5. 將只在特定狀態成立的提示改為條件式顯示。
6. 保留錯誤訊息、成功訊息、不可逆操作後果、金額統計口徑與異常處理指引。
7. 維持現有 API、資料庫、權限、業務規則與操作流程。
8. 維持 visible label、per-field error、heading hierarchy、`aria-describedby` 與 screen reader 可理解性。
9. 驗證 Desktop、Mobile、light mode、dark mode 與三角色主要流程沒有因文案刪減而變得不清楚。
10. 更新 `UI.md`，將 UI Copy 原則納入長期 Design System。

---

## 4. 硬性邊界

v1.6 是純前端 Presentation Layer 與 UX Copy cleanup。

### 4.1 不得修改

- Database Schema 或 migration。
- Backend API request／response 契約。
- Auth、Policy、Middleware、Resource 或角色權限。
- 車輛、收支、薪資、客戶與帳號的正式 Business Logic。
- approved-only 金額口徑。
- Dashboard 統計公式與 URL Filter 契約。
- idempotency、transaction 或資料保護規則。
- 現有頁面路由與模組資訊架構。
- v1.4 的 RWD、Safe Area、Mobile Drawer 與 light／dark mode 契約。
- v1.5 的登入、首次改密碼與自助帳號流程。

### 4.2 不得藉機新增

- 新 Dashboard KPI、圖表、報表或通知。
- Onboarding Tour、教學精靈或第一次登入導覽。
- Tooltip 系統、Popover 教學系統或大量資訊 icon。
- CMS、Database-backed UI copy 或線上系統設定。
- 全新 Design System、Storybook 或平行元件庫。
- Toast framework 或新的 runtime UI dependency。
- 新表單流程、Wizard、Modal workflow 或新的操作入口。
- 即時 on-blur validation 重構。
- 後端為了顯示文案而新增欄位。

### 4.3 允許的程式變更

- 移除或縮短既有 JSX 文字。
- 調整文字容器、間距與必要的 typography class。
- 依前端已經擁有的狀態，將既有提示改為條件式渲染。
- 將視覺上不需常駐的描述保留為 `sr-only`，前提是 screen reader 確實仍需要該資訊。
- 合併同一區塊內重複的統計口徑說明。
- 抽取少量、具有至少兩個實際使用處的共用呈現元件或文案 helper；不得建立萬用 Copy 元件。
- 更新受文案影響的前端測試與 accessible query。
- 更新 `UI.md`、README、current-state、smoke report 與 handoff。

---

## 5. UI Copy 分類規則

所有盤點到的使用者可見文字，必須歸入以下其中一類。

### 5.1 A 類：直接刪除

符合任一條件即可刪除：

- 只是重述頁面標題。
- 只是告訴使用者「可以在這裡管理某項資料」。
- 只是說明可點擊的卡片可以前往對應頁面。
- 與欄位 label、按鈕文字或狀態 Badge 意思相同。
- 操作控制已清楚呈現，文字只是再描述一次控制位置。
- 只對第一次使用有幫助，對日常操作沒有持續價值。

範例：

```text
員工／帳號管理
建立與管理員工帳號、角色與基本資料
```

保留標題即可。

### 5.2 B 類：縮短後保留

適用於：

- 規則確實需要在操作前知道。
- 但現有文字混入太多格式細節、重複句或實作資訊。

原則：

- 一次只表達一個主要意思。
- 優先使用一行短句。
- 不用完整描述後端如何運作。
- 不把所有驗證規則一次列完；錯誤時再顯示具體修正方式。

範例：

```text
帳號名稱可用於登入；未設定時仍可使用 Email。
```

可縮短為：

```text
未設定帳號名稱時，可使用 Email 登入。
```

### 5.3 C 類：改為條件式顯示

適用於只在特定狀態成立的資訊：

- 當月尚未結束，因此薪資只能預覽與重算。
- 方案已被使用，因此永久唯讀。
- 車輛資料缺少獎金歸屬。
- 某操作因目前狀態或權限不可用。
- 使用者尚未設定 username。
- 系統目前處於 error、empty、loading 或 stale state。

不得在正常狀態常駐顯示預防性警告。

### 5.4 D 類：必須保留

以下資訊不得為了畫面乾淨而刪除：

- 會改變金額理解的統計口徑。
- approved-only、成交月份、期末餘額等不符合一般直覺的定義。
- 不可逆操作或資料鎖定的後果。
- 刪除、停用、駁回、重設密碼、確認薪資、發薪等高風險操作說明。
- 成交日期對薪資獎金月份的影響。
- 錯誤訊息與可採取的修正方式。
- 成功後需重新登入、既有 Session 失效等重要結果。
- 權限不足或資料異常時的原因。
- 無資料、無搜尋結果與載入失敗狀態。

D 類文案可以縮短，但不能消失或被藏在只有 hover 才能看到的 tooltip。

### 5.5 E 類：無障礙專用

視覺上可移除，但 assistive technology 仍需要的描述，可以改為 `sr-only`。

適用情境：

- 圖表的統計對象與單位。
- icon-only action 的 accessible name。
- 輸入欄位必要的格式或錯誤關聯。
- 視覺卡片已能辨識，但 screen reader 仍需要補充其導向目的。

不得把重要風險或一般使用者也需要知道的規則只放在 `sr-only`。

---

## 6. 文案設計原則

### 6.1 每個區塊只保留一個主要訊息

同一區塊不得同時以標題、副標、Card description 與 footer 重複相同定義。

優先順序：

```text
清楚的標題
→ 正式數值或內容
→ 必要時一個短句
→ 只有例外時才出現 warning／error
```

### 6.2 控制項應自行表達用途

- 按鈕文字應直接描述動作。
- 欄位使用 visible label。
- 狀態使用 Badge。
- placeholder 只提供範例，不代替 label 或長期規則。
- 可以由控制項清楚表達的資訊，不再加一段「請使用某按鈕操作」的說明。

### 6.3 規格文字不直接傾倒到 UI

開發文件中的完整規則，不代表都要常駐顯示在產品畫面。

UI 文案只回答使用者當下需要知道的問題：

- 這是什麼？
- 現在可以做什麼？
- 為什麼不能做？
- 這個動作有什麼重要後果？
- 發生錯誤後該怎麼修正？

後端演算法、資料來源、完整允許字元與內部狀態名稱，不應在沒有必要時全部顯示。

### 6.4 不依賴 Tooltip 解決資訊過多

Tooltip 有 hover、touch、keyboard 與可發現性問題。

v1.6 不建立大量 `i` icon，把被刪除的文字全部塞進 tooltip。只有非關鍵補充且現有元件已具備完整 mouse／keyboard／touch 行為時，才可使用既有 pattern。

重要規則必須直接可見，或在條件成立時直接顯示。

### 6.5 文字風格

- 使用簡潔、直接的繁體中文。
- 優先使用日常車行與營運用語。
- 不使用行銷式、鼓勵式或聊天式文案。
- 不以「您可以在這裡……」「請注意……」作為所有提示的固定開頭。
- 不使用驚嘆號製造強度。
- 同一概念在不同頁面使用同一名詞。
- 避免把英文技術字詞直接暴露給一般使用者；Email、username 等既有產品用語除外。
- 錯誤訊息說明原因與下一步，不責怪使用者。

---

## 7. Dashboard 調整方向

Dashboard 是 v1.6 優先處理頁面。

### 7.1 工作概況

建議保留：

- 區塊標題。
- KPI 名稱。
- 正式數值與單位。
- 導向既有 Filter 的整卡互動。

建議移除：

- 「點選卡片前往對應工作區處理」。
- 「整備尚未完成」。
- 「整備完成，等待上架」。
- 「已保留，等待完成交車」。
- 「等待核准或駁回」。

上述內容已由 KPI 名稱表達。

### 7.2 經營概況

經營概況的金額口徑仍然重要，但不應每張卡重複。

建議：

- 保留一個區塊級短說明，統一表達「本月金額僅計已核准收支」。
- 「現金帳面餘額」若名稱已足夠，不再重複「所有已核准現金收支」；若實際使用者仍可能誤解，可保留更短說明。
- 移除「完整當月」等與「本月」標題重複的文字。
- 本月成交與本月毛利仍需依既有 `sold_month` 契約導向，不得因刪文案改變 Filter。

### 7.3 趨勢分析

- 視覺上優先保留圖表名稱、日期範圍與單位。
- 重複的 description 可移除或改為 `sr-only`。
- approved-only 毛利與現金期末餘額等非直覺定義，仍需以一個不重複的方式保留。
- 不修改 SVG、資料點、日期、空資料與角色遮蔽行為。

---

## 8. Auth 與我的帳號

### 8.1 Login

- 保留登入識別的必要資訊。
- 不增加登入教學、忘記密碼入口或帳號建立說明。
- 錯誤登入仍使用通用訊息，不得因 UX Copy cleanup 洩漏帳號是否存在。

### 8.2 強制改密碼頁

這是例外狀態，不屬於應完全安靜的正常頁面。

必須保留：

- 為什麼必須先改密碼。
- 完成後才能進入營運功能。
- 密碼更新成功或失敗的結果。

可以縮短長句，但不能讓使用者誤以為可以跳過。

### 8.3 我的帳號

username 區塊：

- 常駐說明只保留 username 與 Email 登入關係。
- 字數、允許字元、轉小寫等完整規則不必全部常駐。
- 格式錯誤時由 per-field error 說明具體修正方式。
- 清除 username 的結果若非直覺，可以保留一句短說明或只在已有 username 時顯示。

密碼區塊：

- 「至少 8 個字元」可以保留在新密碼欄附近。
- 「不可與目前密碼相同」可在提交錯誤時具體顯示；若後端正式規則要求操作前必須知道，可以與字數規則合併為一行。
- 不改變自助密碼更新、Session stale handling 與重新登入流程。

---

## 9. 員工／帳號管理

### 9.1 頁面層級

- 保留單一 `h1` 與主要動作「新增員工」。
- 移除重述頁面用途的副標。
- 非 admin 正常不應進入該頁；既有權限保護不由 v1.6 變更。

### 9.2 編輯與列表

- 移除「角色請使用列表中的下拉選單」等描述現有控制位置的教學句。
- 保留角色、啟用狀態、需改密碼等正式狀態。
- 不因減字改變自己帳號的角色、停用、刪除與重設密碼保護。
- 自己無法從管理頁重設密碼的原因仍需可理解，但不得用一整行常駐文字增加每列高度；實作時應選擇既有且 accessible 的最小呈現方式。
- 重設密碼後造成 Session 失效與再次強制改密碼的成功結果必須保留。

---

## 10. 薪資模組

薪資包含較多非直覺規則，因此不能採取全面刪除。

### 10.1 一般原則

- 正常、可操作狀態不顯示預防性長文。
- 只有特定狀態成立時才顯示 warning。
- Warning 一次只說明主要原因與下一步。
- 不阻擋型提示與阻擋型錯誤需維持不同視覺語意。

### 10.2 必須保留

- 當月尚未結束，只能預覽與重算，不能確認。
- 已使用獎金方案永久唯讀，規則變更需建立新版本。
- 來源未確認、歸屬不完整或資料異常造成的處理方式。
- confirmed／paid 的歷史鎖定。
- 發薪的正式帳戶與日期後果。
- 公司保留、分配池、獎金與公司淨得的正式名詞。

### 10.3 可精簡

- 同一頁重複解釋月份狀態。
- 標題已明確表達的副標。
- 正常狀態下仍常駐的預防性設定提示。
- 同一異常在 summary、明細與 footer 重複顯示的文字。

不得修改獎金計算、eligible 規則、跨級台數、月份鎖定或發薪 MoneyEntry。

---

## 11. 車輛、收支、客戶與資金帳戶

### 11.1 車輛

- 不改車輛狀態名稱、操作流程或狀態按鈕可見性。
- 成交日期影響薪資月份的提示屬高風險資訊，必須保留但可縮短。
- 照片上傳、封面、排序與失敗狀態的必要提示保留。
- 一般操作按鈕已清楚時，不再加一段描述按鈕用途。
- 車輛 Card 空圖「尚無照片」屬必要 empty state，保留。

### 11.2 收支

- 一般營運收支與單車收支的關聯規則不可刪到無法理解。
- v1.6 不修改分類選擇、車輛欄位 required 狀態或 payload。
- 現有長句可以縮成一行，或在使用者真的選擇矛盾資料時由錯誤訊息說明。
- pending／approved／rejected、核准與駁回結果必須清楚。
- 權限與金額遮蔽不變。

### 11.3 客戶

- 移除重述「建立／管理客戶」的頁面副標。
- 保留空狀態、搜尋結果與刪除／更新錯誤。
- CustomerSelect placeholder 不得取代 label；既有帶入或手動輸入提示只在確實避免錯誤時保留。

### 11.4 資金帳戶

- 移除描述列表按鈕位置的教學文字。
- 保留帳戶停用後的影響與高風險操作提示。
- 正式餘額定義不可被改成前端估算。

---

## 12. Audit、Print 與其他頁面

### 12.1 Audit Log

- 「追蹤登入、資料新增、修改與刪除」若只重述頁面用途，可移除或縮短。
- 「紀錄僅供查詢」若代表不可修改的重要邊界，可保留為短句。
- 無欄位異動、請求資訊與 empty state 必須清楚。

### 12.2 Print

- 不因文案精簡刪除正式欄位名稱、收支說明或列印辨識資訊。
- 列印頁不是本版本的主要 copy cleanup 對象。
- 只修正明顯重複或不必要的畫面說明，不改列印資料結構與 CSS 版面。

### 12.3 全站狀態文字

統一檢查：

- Loading
- Empty state
- No results
- Error
- Success
- Warning
- Disabled reason

同一情境應使用一致名詞，但不得為了「統一」而建立過度抽象的全站訊息系統。

---

## 13. Accessibility 邊界

v1.6 的「減字」不得犧牲可用性與無障礙。

必須維持：

- 每頁唯一 `h1` 與正確 heading hierarchy。
- 每個表單欄位 visible label。
- placeholder 不取代 label。
- API per-field error 與對應欄位的 `aria-describedby`。
- `aria-invalid`、required marker 與 focus behavior。
- Button 與 Link 的 accessible name。
- Modal／Drawer 的標題、focus trap、Escape 與 focus return。
- Loading、error、success 的適當 `aria-live`。
- 圖表仍能表達名稱、期間、數值與單位。

刪除 help text 時，必須同步檢查：

- 是否留下指向不存在 ID 的 `aria-describedby`。
- 測試是否只因文字被刪而失去可存取的 selector。
- 讀屏使用者是否仍能知道非直覺的格式或操作後果。
- `title` 是否被誤當成唯一提示來源。

---

## 14. Responsive 與視覺密度

v1.6 的主要改善之一是降低 Mobile 文字高度，但不得用縮小字體解決。

正式規則：

- 不把中文輔助文字縮到 12px 以下作為主要內容。
- 優先刪除重複內容，而不是壓縮 line-height。
- Desktop 不能因減字出現不平衡的大面積空白或 Card 高度跳動。
- KPI Grid、Table、Form、Modal 與 Drawer 的既有 breakpoint 行為不變。
- 需驗收 320、375、390、768、1440px。
- light／dark mode 均需檢查文字對比、warning 層級與空狀態。
- iPhone Safari Safe Area、Sidebar overlay 與背景同步不得回歸。

---

## 15. 測試要求

### 15.1 Frontend automated tests

必須執行：

- `npm test`
- `npm run lint`
- `npm run typecheck`
- `npm run build`

必須更新或補充：

- Dashboard 重要標題、KPI 導向與角色可見性測試。
- Account username help／error 的 accessible 關聯。
- Password required flow 的必要說明仍存在。
- User 管理的高風險狀態與成功訊息。
- Salary 條件式 warning 的顯示／不顯示。
- 刪除文字後不留下失效的 `aria-describedby`。

不得只用 snapshot 證明文案品質。測試應驗證正式互動、條件與 accessible contract。

### 15.2 Backend regression

v1.6 原則上不應有 backend diff。

封板前仍需：

- 以 Git diff 確認 backend、migration 與 API contract 未被修改。
- 執行既有 backend full suite，確認沒有基準回歸。

不需要重跑 MySQL／MariaDB 專用競態測試，前提是本版本沒有任何 backend、schema、transaction、time zone 或金流變更。若實際 diff 超出此條件，必須停止並重新評估版本範圍。

---

## 16. Browser Manual Smoke

Browser Smoke 必須用真實 API 與三角色執行，不使用假資料。

### 16.1 共同檢查

- 頁面標題與主要動作一眼可辨識。
- 移除副標後仍知道目前頁面與可執行操作。
- 正常狀態沒有大量重複說明。
- 錯誤、warning、disabled reason 與不可逆後果仍清楚。
- 無整頁水平 overflow。
- 鍵盤 focus 與 Tab 順序正常。
- light／dark mode 文字層級清楚。

### 16.2 角色

Admin：

- Dashboard 財務與待審核 KPI。
- 員工管理、資金帳戶、薪資、Audit。
- 高風險管理操作的提示仍清楚。

Manager：

- Dashboard 財務資訊。
- 車輛、收支、客戶流程。
- 不顯示 admin-only 管理說明或入口。

Sales：

- Dashboard 不洩漏財務 KPI。
- 車輛與客戶主要流程可理解。
- 不因刪除說明而誤導可見或不可見權限。

### 16.3 狀態

至少檢查：

- Loading
- Empty
- Filter no result
- API validation error
- API general error
- Success
- Disabled
- Warning
- Mobile Drawer／Modal

### 16.4 裝置與寬度

- Desktop Chrome 或 Firefox：1440px。
- Tablet：768px。
- Mobile：320、375、390px。
- 使用者真實 iPhone Safari 複驗主要頁面與 Safe Area。

---

## 17. 文件要求

v1.6 完成時必須更新：

- `UI.md`：新增 UI Copy、helper text、warning 與 accessibility 規範。
- `README.md`：更新版本狀態與驗證方式。
- `docs/current-state.md`：記錄 v1.6 正式狀態與基準。
- `docs/v1.6-smoke-report.md`：記錄 automated 與 browser smoke。
- `docs/v1.6-handoff.md`：記錄完成範圍、未做項目與後續部署邊界。
- `AGENTS.md`、`CLAUDE.md`：由規劃狀態更新為完成狀態。

v1.6 不需要更新 `backend/API.md`，前提是實際沒有 API 契約變更。若實作過程認為需要修改 API，必須停止並先取得使用者決策，不得自行擴張。

---

## 18. 正式範圍

### 18.1 必做

- 全站可見輔助文案盤點。
- 文案五分類與逐頁決策。
- Dashboard 精簡。
- Auth／Account／User 管理精簡。
- Salary 高風險提示分層。
- Vehicle／MoneyEntry／Customer／CashAccount 精簡。
- Audit 與其他頁面收尾。
- Accessibility 回歸。
- RWD、light／dark mode 與 Browser Smoke。
- `UI.md`、測試、smoke report、handoff 與 current-state 同步。

### 18.2 明確不做

- 新商業功能。
- Backend、API、Schema 或 migration 修改。
- 新角色、權限或資料遮蔽規則。
- 新 Dashboard 指標、報表或圖表。
- 新通知、教學導覽或 onboarding。
- Tooltip／Popover 平台。
- 全站表單驗證重構。
- 完整 Design System 重寫。
- 品牌、配色、Sidebar、Navigation 或整體版型改版。
- 官網、部署、R2、備份或正式環境建置。
- v1.1～v1.5 既有功能回開。

---

## 19. 完成定義

v1.6 只有在以下條件全部成立時才可封板：

- 所有前端正式頁面已完成文案盤點，不只修改使用者最先指出的頁面。
- 每一處刪除都能說明其資訊已由標題、控制項、狀態或條件式提示取代，而不是單純追求少字。
- Dashboard 的重複 description 已明顯減少，金額口徑仍可理解。
- 正常狀態不再常駐顯示大量教學文字。
- 高風險、不可逆、金額口徑、錯誤與異常提示仍完整。
- Auth、Vehicle、MoneyEntry、Salary 與 User 的正式流程沒有改變。
- Backend、API、Schema、權限與資料遮蔽沒有 diff。
- Frontend test、lint、typecheck、build 全數通過。
- Backend full suite 通過；MySQL-only tests 若未執行，需明確記錄本版本不涉及其風險範圍。
- 320／375／390／768／1440px、light／dark mode 與三角色 Browser Smoke 通過。
- 使用者完成主要頁面的真實裝置複驗。
- `UI.md`、README、current-state、smoke report、handoff、AGENTS 與 CLAUDE 同步。
- 使用者明確授權後，才可建立 `v1.6-smoke-passed` annotated tag。
